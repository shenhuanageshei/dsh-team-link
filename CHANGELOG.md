# Changelog

本文件记录 `dsh-team-link` 的变更史。版本号策略：`package.json` 的版本号**随发布统一 bump**——开发期累积的条目先记为「未发布」，发布时一次性收口（例如 0.3.1–0.3.6 的条目在 0.3.7 发布时一并落定）。

格式：每个版本按 **修了什么 → 为什么 → 怎么验证** 组织。凡涉及行为修复的条目都附**变异验证**证据（修复前必红 / 修复后全绿），这是本仓库的验收文化。

---

## 未发布 — B 批收尾修复轮（代码评审 R1–R6）（2026-09-27）

> 形态批（B 批）与分歧审计修复轮 `DIVERGENCE(8)` 之后的**收尾修复轮**：只取代码评审 7 条里的 6 条（R1–R6），**R7 仅留档、代码与夹具一字未动**（父侧裁定：接受顺序耦合，理由见验证档的「已知可接受项」）。**不夹带新功能、不扩范围**：A 批设计 §7 与 B 档 §7「明确不做」继续有效；**不动 `docs/observability-batch-design-2026-09-26.md`**；**不碰部署面**。逐条读数与红/负相拆分见 [`docs/verification-log.md`](docs/verification-log.md) 的「B 批收尾修复轮（R1–R6）」一节。

### ✨ 修了什么

**① R1 —— 诊断话术仍是断言式归因（D1 同类漏改的一处）** —— `probeAgentTeams` 的 service-absent 分支把原因写成因果断言「宿主没有提供 agentTeams 服务（**本次运行的 profile** 没有装载 …组合包）」。而 cordis 的 `ctx.get` 只返回**已激活**的 provider：一个尚未完成 `[Service.init]` 的 `agentTeams` 也会走到这一支 ⇒ 「profile 没装载」是**假阴性断言**（正是 D1 要消灭的那类错误归因）。现在该 reason 软化为「服务 `agentTeams` 当前不可见（可能未装载 …组合包，也可能尚未激活完成 —— 可稍后重试，或核对当前活动 profile 的 `dsh.profile.bundles`）」：写路径的拒绝照旧刺眼（拒绝 + 指路 + 零写入），**但不替宿主下结论**。引用该文案的既有断言（`D2 首行按分支准确化（反向锁）`）同批改写。

**② R2 —— 同一判据被探测两次** —— `set-mode` 的可用性门与确认框正文的投影各调一次 `probeAgentTeams`。现在「切到 agent-team」那一向的探测结果**直接复用**为确认框正文的投影（`probe ?? probeAgentTeams(...)`）；只有「切回 sessions」那一向没有前置探测，才现读一次。两条路的处置仍各自决定（可用性门 vs. 正文那一行），判据仍只此一处。

**③ R3 —— TOCTOU 复检后的不对称（账与镜像可能分家）** —— 写时复检之后，镜像用**复检后**的行，而形态史那行还用**对话框前**的 `team`：用户在确认框打开期间经设置 UI 改了 workspace 时，镜像写新路径、账写旧路径。现在两者**同源**（同一个 `teamWritten = nextTeams.find(...)` 行）。

**④ R4 —— 幂等放宽的语义噪音（文档侧 + 返回体措辞同批）** —— 从未切换过的团队第一次收到 `set-mode mode=sessions`（其默认档）会**补记一条「形态 → sessions」**：为一次从未发生的切换造一行史（属设计裁定的取舍，**行为不动**）。文档与返回体一律改准为：**首次声明也算一行** —— 形态史记的是「**声明过这一档**」，不是「档位发生变化」。旧措辞「形态史记的是「切换」，不是「重复声明」」在 B 档 / README / `lib/index.js` 三处**零残留**。

**⑤ R5 —— 无会话身份时诊断行会「猜」** —— `otherReachableSessions` 在调用方无会话身份时退化为按 `process.cwd()` 过滤 ⇒ 可能触发**基于猜测**的「⚠ 多会话通道看起来不可用」，与同文件「读不到 ⇒ `null` ⇒ 不判、不猜」的纪律不一致。现在 `selfId === undefined` ⇒ **返回 `null`（不判）**，与 `sessionIdSnapshot` 的失败语义对齐；状态卡 ⑦ 段（原先直接传 `pool`）随之收敛到同一个判据。

**⑥ R6 —— 状态行措辞过宽** —— B 档状态行写「**D8 是本轮唯一的行为改动**」，但 D2（读面首行按分支取）与 D3（镜像新增两行 + `set-mode` 落笔后重写镜像）都改了**可观测输出**。同批收窄为「唯一的**门 / 写路径语义**改动（D2 / D3 只改读面文案与镜像渲染）」，B 档 §4.2 与上一条 `DIVERGENCE(8)` 条目里的同句声明一起改准。

### 💡 为什么

R1 / R5 是同一个病：**把观测到的现象说成了因果结论**（R1：「服务看不见」⇒ 断言「profile 没装载」；R5：没有身份 ⇒ 拿 `process.cwd()` 过滤出来的空列表断言「多会话通道不可用」）。本仓的家规是「**读不到 ⇒ null ⇒ 不判、不猜**」，所以两处都改成只报现象、只在不猜的前提下下判据。R2 / R3 是同一个形状的另一面：**同一份事实被取了两次**（R2 探测、R3 行来源），于是两次取到的东西可以不一样 —— 一次取、一处用，分家就不可能。R4 / R6 是文档与实现的自相矛盾（R4：文档说「切换才记一行」而实现连首次声明也补记；R6：说「唯一的行为改动」而同轮还有两处可观测输出变更）。

### 🚫 明确不做（本轮）

**R7（测试夹具顺序耦合）不接受修复** —— 父侧裁定「可接受的顺序耦合（有注释、teardown 已清理）」，本轮只在验证档留一条「已知可接受项」并给出将来的做法（重排用例时给各用例独立 workspace；D8 的 `nbWs` / `fbWs` 已是该做法），**代码与夹具一字未动**。其余同 `DIVERGENCE(8)`：不加新工具 / 新参数 / 新事件类型 / 新持久状态；不动投递层与黑板三件套的语义；**不动 `docs/observability-batch-design-2026-09-26.md`**；**不碰部署面**（`profile/profiles/web/...` 与用户目录里的插件副本）。

### 🧪 怎么验证

- **命令**：`node host-half.test.mjs && node client-half.test.mjs`；
- **绿相（实跑两行原文）**：`node host-half.test.mjs` → `ALL PASS / assertion total: 1193 (failed: 0)`（退出码 0）；`node client-half.test.mjs` → `ALL PASS / assertion total: 269 (failed: 0)`（退出码 0）。基线 `1184` / `269` ⇒ **新增 9 条宿主断言**（`1193 = 1184 + 9`）＋ **改写 1 条**；**浏览器半边本轮一字未改**（仍 `269` —— 本轮只动宿主半边）；
- **红相（本轮修复前的实现 ＋ 本轮的新断言/改写断言；测试先行）**：`assertion total: 1193 (failed: 8)`（`nodeExit=1`）—— **8 条真红**（7 条新断言 ＋ 1 条改写的反向锁）；另 **2 条新断言在两种实现上都绿**，如实标 **★ 负相**（`R2 ★ 负相（切回方向）` = 覆盖缺口类；`R5 反面对照` = 反向锁），**不冒充红相**；
- **回归**：B 批 41 条、`DIVERGENCE(8)` 16 条与 A 批 / 台账批的既有断言**零回归**（本轮唯一改动的既有断言就是 `D2 首行按分支准确化（反向锁）` 的措辞）。

### 🔁 同批同步的面

`lib/index.js`（`probeAgentTeams` 的 reason · `set-mode` 的探测复用与 `teamWritten` 同源 · 两处形态史措辞 · `otherReachableSessions` 的 `selfId` 早返回 · 状态卡 ⑦ 段的判据）；`host-half.test.mjs`（9 条新断言 ＋ 1 条改写；新夹具各自一份 workspace）；`README.md`（三处读数 `1184 → 1193` · 幂等行 · 宿主探测 / 落笔 / 形态诊断行三行的口径）；本条目；`docs/verification-log.md`（本轮一节）；`docs/team-mode-batch-design-2026-09-26.md`（状态行 · §4.2 幂等口径）；`docs/team-ledger-and-mode-design-2026-09-26.md`（§7 步 3 行的本轮交付说明）。

---

## 未发布 — 分歧审计修复轮 `DIVERGENCE(8)`（2026-09-27）

> 只读分歧审计的 **8 条**结论（D1–D8）逐条闭环。**不夹带新功能、不扩范围**：A 批设计 §7 与 B 档 §7「明确不做」继续有效。逐条验证证据见 [`docs/verification-log.md`](docs/verification-log.md) 的「分歧审计修复轮 `DIVERGENCE(8)`」一节。

### ✨ 修了什么

**① D1 —— 文档把真机读反了（实证）：文案通用化 + 补设置 UI 兜底路** —— 旧文案断言「**本部署未装载** `@deepseek-ai/dsh-experimental-agent-team-profile` ⇒ `ctx.get("agentTeams")` 为 undefined ⇒ 切档被拒」。实测事实是：本部署跑的是 **desktop profile**（`DSH_PROFILE=desktop`），其 `dsh.profile.bundles` **含**该组合包；本会话系统提示词里同时有工具包 POLICY 常量与 `spawn_teammate` 工具声明（该工具只由 `@deepseek-ai/dsh-experimental-agent-team-tool-agent-team` 注册，其 `inject` 含 `agentTeams`）⇒ **服务已在运行实例中挂载** ⇒ 探针 `available:true` ⇒ **U11b 的拒绝路径在本部署不会触发、US1 可达**。于是：`AGENT_TEAMS_HOWTO` 改成**通用表述**（按**当前活动 profile** 的 `dsh.profile.bundles` 自查；已经在那里 ⇒ 拦住本次切换的是别的原因），拒绝文案里**补一行设置 UI 的兜底路**（`MODE_SETTINGS_FALLBACK`：形态就是 settings 的 `team-link.teams[].mode` / `leadSessionId`，用户经设置 UI 永远是超级写者）—— 防一次假阴性把用户卡死。读面首行同时按分支准确化（见 ②）。

**② D2 —— 首行与原因行自相矛盾（真机最常走到的那一支）+ 两条断言** —— 名册投影不可读时原先**无条件**先打常量「成员名册不可读（本部署未提供该投影）」，再打原因行；而探针有五条不可读分支（服务缺席 / 缺方法 / 无会话身份 / **调用方非成员** / 读取抛错），真机走的是「服务在场、调用方不是成员」那一支 ⇒ 首行与原因行**互相打脸**。现在探针回一个 `cause` 判别符，渲染器据此取首行：只有 `service-absent` 说「本部署未提供该投影」，其余四条用**中性首行**（`成员名册不可读 —— 仍可读 Lead 与形态（哪一条读不到，见下一行的原因）`）+ 紧随的`（不可读原因：…）`行。**补两条断言**：「调用方非成员」档与「读取抛错」档（此前**零断言**）。

**③ D3 —— roster.md 镜像补 mode / Lead 两行 + `set-mode` 落笔后重写镜像** —— 镜像文件自述「与**设置变更**同一事务内 best-effort 镜像」，但 `set-mode` 既不渲染这两个字段、也不重写镜像 ⇒ 那句自述对本字段是**假话**。现在镜像里出现`- 形态（mode）：…`与`- Lead 会话（leadSessionId）：…`两行，且 `set-mode` 落笔后与既有调用点（`upsert-team` / `set-role` / `retire`）**同形**地调用同一个 `writeRosterMirror`（best-effort 语义一字不差：镜像坏掉只告警，绝不把整次切换变成失败）。

**④ D4 —— 过期限定词统一（三处）** —— 父档 §2.3 矩阵第 7 行、`lib/index.js` 的能力矩阵常量、README 的同一张表此前仍写「成员部分要读宿主投影（**未复核**）」，而同批已把父档 §8 改成「✅ 已核」；B 批「逐行照抄」把它抄进了代码常量。三处统一为已核口径：`⚠️ 成员部分读宿主投影（2026-09-27 已核：本部署已挂载该服务 ⇒ 探针 available=true）`。

**⑤ D5 —— 补确认框「成员名册不可读」那一档的断言** —— §4.4 写死了两档（可读 ⇒ 列成员 / 不可读 ⇒ 写「（成员名册不可读）」），但套件只断言了可读档。现已补上（含「不会迁移的三件事」与「不阻断」两句的对照）。

**⑥ D6 —— 「只改这两个键」的表述改准 + 多团队夹具** —— README / CHANGELOG 原写「除这两个键以外**一个字不改**」，而实现是 `reopened.teams.map(...)` **整份数组回写** ⇒ 其余团队行也会被**补默认值**（`mode:"sessions"` / `leadSessionId:""`）。现在如实写成「只改**本团队行**的两个键；其余团队行**已有值不被覆盖、缺值被补默认**（与既有 `upsert-team` 同形，语义无变化）」，并补一条**多团队夹具断言**钉住这两半。

**⑦ D7 —— 测试加固三处** —— ① 能力矩阵断言由「4 个 ❌ ＋ 3 个 ⚠️ ＋ 其中两行原文」改为**逐行锁八行原文**（其余六行的文本漂移此前**不会红**，是真盲区）；② 删掉 `modeSetEnv({ tag: "rg" })` 的死参数（该函数没有 `tag` 形参）；③ 三道门的 `length` 断言在注释里**如实标注**为「函数元数变更探测器」，不是行为锁。

**⑧ D8 —— 唯一的门 / 写路径语义改动（D2 / D3 只改读面文案与镜像渲染）：让「补记」这条路真的可执行（原指路与幂等互斥）** —— `decisions.md` 写失败时给出的指路是「补写路径后**重发本次切换以补记**」，但幂等分支（同档同 Lead）会**先命中**并返回「已是该档」、**连 decisions 都不追加** ⇒ 该指路**不可执行**，US6「每次切换留一行」会被永久破坏。现在幂等判据放宽为「同档同 Lead **且账上已有这一档的形态史行** ⇒ 已是该档（零写）；**账上缺这一行 ⇒ 补记恰好一行**再返回（返回体如实说明这是补记）」。形态史行的识别走**共享常量函数** `modeHistoryBody`（写读同源：落笔与判据读同一个函数），只比正文、不看时间戳与作者；**读不出来 / 没有 workspace ⇒ 不补记**并在返回体如实说明（fail-closed，不猜）。

### 💡 为什么

这一轮的八条有同一个形状：**文档/文案断言了没有实测过的结论**（D1 把真机读反了 —— 它以为拒绝路径会被触发，真机其实根本走不到），或者**两条互相矛盾的话被并排写在一起**（D2 的首行与原因行、D6 的「一个字不改」与整份数组回写、D8 的指路与幂等）。处置原则沿用本仓家规：**能实测的就实测**（D1 四处改成真机事实，并让文案只说它在当前 profile 下该说的话）、**能标注的就不沉默**（D2 的中性首行 + 原因行、D8 的「不补记」）、**能红的断言不许留盲区**（D7 逐行锁 + D5 补档 + D8 两条新判据）。

### 🚫 明确不做（本轮）

不加新工具 / 新参数 / 新事件类型 / 新持久状态；不改投递层与黑板三件套的语义；**不动 `docs/observability-batch-design-2026-09-26.md`**；不做跨形态改投、不做自动切档、不代理宿主的派活/任务板（B 档 §7 继续有效）；**不碰部署面**（`profile/profiles/web/...` 与用户目录里的插件副本）。

### 🧪 怎么验证

- **命令**：`node host-half.test.mjs && node client-half.test.mjs`；
- **绿相（实跑两行原文）**：`node host-half.test.mjs` → `ALL PASS / assertion total: 1184 (failed: 0)`（退出码 0）；`node client-half.test.mjs` → `ALL PASS / assertion total: 269 (failed: 0)`（退出码 0）。基线（本轮修复前的工作树）`1168` / `269` ⇒ **新增 16 条宿主断言**（`1184 = 1168 + 16`）；**浏览器半边本轮一字未改**（仍 `269`）；
- **红相（本轮修复前的实现 ＋ 本轮新断言；测试先行）**：**`assertion total: 1184 (failed: 14)`**。逐条点名后**做了归因**：**13 条是真红**（D1 两条 · D2 两条分支 · D3 三条 · D8 三条 · U6 两条幂等 · U11 矩阵逐行锁一条），第 14 条（`D6 多团队夹具`）是**夹具假红**（那一版夹具漏了宿主替身 ⇒ 切换先被探测拒掉），补上替身后它**在两种实现上都绿**、改归 ★ 负相；另 **6 条在修复前本来就绿**，如实标 **★ 负相**（覆盖缺口类：`U6 账上已有该行`【改写】· `D2 首行反向锁` · `D5 不可读档` · `D5 两档对照` · `D8 前置（写失败）` · `D6 多团队夹具`）—— 不冒充红相；
- **本轮改写的既有断言 3 条**（同批同步，不留旧口径）：`U11 完整能力矩阵`（→ 逐行锁八行原文）· `U6 幂等（agent-team）`（→ 加上「账上已有该行」这一半）· `U6 幂等（sessions）`（→ 改成「账上缺该行 ⇒ 补记恰好一行」＋「补记之后 ⇒ 零写幂等」两条）；
- **回归**：B 批 41 条与 A 批 / 台账批的既有断言零回归（唯一的三处改动就是上面那三条改写）；模块级 `inject` 仍 4 项；`policy` 命名空间键集不变（零新增持久状态）；D8 的两个写路径都只碰 `decisions.md`（只追加一行，不重写）。

### 🔁 同批同步的面

`lib/index.js`（`AGENT_TEAMS_HOWTO` 通用化 + `MODE_SETTINGS_FALLBACK` + 拒绝文案 + 能力矩阵第 7 行 + `probeAgentTeams` 的 `cause` + `TEAM_MODE_ROSTER_UNREADABLE_NEUTRAL` + 渲染器按分支取首行 + `renderRosterMirror` 两行 + `set-mode` 的镜像调用 / 幂等放宽 / 形态史共享写入 `modeHistoryBody` / `decisionRowBody` / `decisionsHasModeHistory` / `appendModeHistoryRow` / `backfillModeHistoryRow` + roster 工具描述面）；`host-half.test.mjs`（16 条新断言 ＋ 3 条改写 ＋ `makeAgentTeams` 的 `listThrows` 与 `modeSetEnv` 的 `workspace` 两个夹具参数）；`README.md`（三处读数 `1168 → 1184` · 能力矩阵第 7 行 · 落笔 / 幂等 / 看形态 / `set-mode` 四行 · `agentTeams` 服务表）；本条目；`docs/verification-log.md`（本轮一节）；`docs/team-mode-batch-design-2026-09-26.md`（§3 未复核面行 · §4.2 幂等口径 · §4.3(3) 第三种文案 · §6 U6 · §8 A1 与 §8.1 A1 · §2.3 第 7 行）；`docs/team-ledger-and-mode-design-2026-09-26.md`（§2.3 第 7 行 · §8 已核那条）。

---
## 未发布 — 形态批（B 批：团队形态 · `set-mode` · 读面形态段 · teammate 交卷提示）（2026-09-27）

> 设计（唯一事实源）：[`docs/team-mode-batch-design-2026-09-26.md`](docs/team-mode-batch-design-2026-09-26.md)（§4.1 形态字段 / §4.2 `set-mode` / §4.3 读面形态段 / §4.4 交卷提示 / §5 红线六条 / §6 判据 **U1–U14 ＋ U11b** / §7 明确不做 / §8 假设 A1–A4）。来源是[《台账 + 形态设计》](docs/team-ledger-and-mode-design-2026-09-26.md) §7 的**步 3**（形态字段 + agent-team 档）与**步 5**（teammate 交卷提示）。逐条红相/绿相读数见 [`docs/verification-log.md`](docs/verification-log.md) 的「形态批（B 批）」一节。

### ✨ 修了什么

**① 形态字段（只落在 settings，§4.1）** —— `TeamConfig` 扩两字段：`mode`（闭集 `sessions`（默认）/ `agent-team`）与 `leadSessionId`（agent-team 档的 **Lead 指针**，不是名册）。**既有团队零迁移**：老 settings 行没有这两个键也照常读出 `sessions`；读到**闭集外**的 `mode` ⇒ 落默认档**并在读面留痕**（点名原值、闭集、以及「原值未被改写」），settings 里的原值一个字不动 —— 与「手改出来的行必须降成**可读状态**，绝不降成不可读命名空间」同一条范式。

**② `team_link_roster action=set-mode`（第五个封闭动词，§4.2）** —— 团队形态在多会话档 ↔ agent-team 档之间切换：

1. **闭集**：`mode` 不在闭集内 ⇒ 拒绝并**列出两个值**（拒绝文案渲染的就是校验用的那一个常量）；
2. **Lead**：`leadSessionId` **省略 ⇒ 默认取调用方自己的会话 id**，并在确认框正文里**明示该默认值**（人可见即可否决）；**显式空串 ⇒ 拒绝**；非空须过**形状**（`^[A-Za-z0-9][A-Za-z0-9._-]{0,63}$`）与**在场校验**（能在本次会话列表快照里找到 —— 防转录错位）两道；
3. **宿主探测只对「切到 agent-team」这一向**：探测不到 ⇒ **拒绝 + 指路怎么开 + 零写入**（切回 `sessions` **不探测**）；
4. **双重门（缺一即 fail-closed、零写入）**：① `writer gate` **原样**（现任协调者；空缺 ⇒ 会话路径拒绝，设置 UI 仍是兜底）；② **人类确认框**（无确认服务 / 超时 / 取消 ⇒ 拒绝且零写入）；
5. **落笔**：settings 的团队行只改 `mode` / `leadSessionId` **两个键**（`teams` 是**整份数组回写**：其余团队行若缺这两个键会被**补默认值**、已有值不被覆盖 —— 与既有 `upsert-team` 同形）+ `decisions.md` 追加**恰好一行**形态史（`形态 → agent-team（Lead=<id>）` / `形态 → sessions`）+ best-effort 重写 `roster.md` 镜像；
6. **幂等**：同档同 Lead **且账上已有这一档的形态史行** ⇒ 「已是该档」，**不弹框、不写 settings、零写入**；账上**缺**那一行 ⇒ **补记恰好一行**再返回（否则那条「补上路径后重发以补记」的指路不可执行），账读不出来 / 没有 workspace ⇒ **不补记**并如实说明；另有写时复检（对话框横跨任意长的人工等待，落笔前的现任条件由宿主**重新观测**）。

**③ 读面形态段（§4.3，`roster get` 与状态卡⑦段同一个渲染器）** —— 形态 + Lead（Lead 为空标「**Lead 未知**」，不编造）+ 成员名册**投影**（**读时现算、一行不落 settings**；来源文案**写死两种**：`来源：宿主 agentTeams 投影（本部署可读）` / `成员名册不可读（本部署未提供该投影）—— 仍可读 Lead 与形态`）+ **完整能力矩阵**（逐行照抄父档 §2.3：四件 ❌ / 三件 ⚠️ / 一件与形态无关）+ 「**单会话兜底档**」那句结论 + **形态诊断行**（除自己外没有其他可达会话时只提示、**绝不自动切**）。

**④ teammate 交卷提示（§4.4，提示式、不阻断）** —— 切换确认框正文列出「**不会迁移的三件事**」（① 信任 ② 成员名册 ③ 任务归属）+ **当前 teammate**（可读则列，不可读如实说）+「请确认它们的结论已落到黑板或文件」。**不逐成员推断有没有交卷**（那要读子代理日志，§7 明确不做）。

### 💡 为什么

形态**不是功能开关，是通道选择**：多会话档给的是「平级会话之间的信任层」（投递 / 配对 / 换届 / 黑板），agent-team 档给的是「一个会话内部的子代理团队」。两档各有各的主人，所以本批只做四件事：**把选择写成 settings 里的一个字段**、**把切换做成一次由人点一下的动作**、**把「这一档下什么用不上」如实摆到读面上**、**把「切走会丢什么」在切之前说出来**。三条纪律贯穿全批：**只读宿主**（不 spawn / 不派活 / 不改宿主任务状态）、**不悄悄降级**（写路径刺眼报错 + 指路；读路径如实标注并保持其余能力 —— **两条路处置不同且不许混用**）、**指针不拷贝**（只存 Lead 会话 id，成员名册读时现算）。

### 🚫 明确不做（本批）

不代理宿主的 spawn / 派活 / 任务板；**不读子代理日志**（交卷只做提示式，不逐成员推断）；**不做自动切档**（切换永远由人触发）；不改 `pairs`/`trustedSenders` 的迁移语义（只**声明**不迁移）；不做客户端面板（数据面先行，与 A 批同一取舍）；**不做跨形态改投**（切档不改投在飞消息、不建桥 —— 要跨形态说话由模型自己用对应形态的手段说）。

### 🧪 怎么验证

- **命令**：`node host-half.test.mjs && node client-half.test.mjs`；
- **绿相（实跑两行原文）**：`node host-half.test.mjs` → `ALL PASS / assertion total: 1168 (failed: 0)`；`node client-half.test.mjs` → `ALL PASS / assertion total: 269 (failed: 0)`。基线 `1127` / `269` ⇒ **新增 41 条宿主断言**（`1168 = 1127 + 41`）；**浏览器半边本批一字未改**（仍 269 —— 设计档 §7 明确不做客户端面板）；
- **红相（本批 41 条判据 ＋ 实施前的 `lib/index.js`）**：**`assertion total: 1168 (failed: 34)`** —— 33 条 B 判据红 ＋ 1 条**既有锁**红（`U10` 的 roster 参数键集锁：临时状态下测试文件已按设计要求期望 `mode`/`leadSessionId`，而旧实现没有这两个参数 —— 那条红正是这条锁该红的理由，与 A 批的「工具计数同改锁」同源，不是回归）；
- **红相的安全方法**：把工作树版 `lib/index.js` **读进内存** → 用 `git show HEAD:lib/index.js` 覆写（**换来换去都有 sha 对照**：覆写后的 `git hash-object` == `git rev-parse HEAD:lib/index.js`，即换进去的就是实施前那一份）→ 跑 `node host-half.test.mjs` → **还原内存里的那一份** → **sha256 逐字节复核**（`restoreMatch: true`，工作树版 sha256 `4110893DC5F07C56909EE194B1E6BF0F20F5C674B8233007065CAE81A378FA3B`）。**不留备份文件、不留探针**，工作树零残留；
- **分阶段红相（测试先行，逐阶段实跑）**：阶段 1 `1137 (failed: 9)` · 阶段 2 `1157 (failed: 19)` · 阶段 3 `1168 (failed: 2)` —— 每一阶段的新断言都是在**该阶段实现之前**写进去并实测红的（逐条红/负相拆分见验证账本）；
- **回归**：既有四动词（`get`/`upsert-team`/`set-role`/`retire`）与 `team_link_rotate`/`team_link_recover` 的动词闭集与三道门函数**一字未动**；模块级 `inject` 仍 4 项；`policy` 顶层键集不变。**唯一改动的既有断言**是上面那条 roster 参数键集锁（它必须跟着参数面一起改，这是本仓「改一处即两处」的既有规矩）。

### 🔁 同批同步的面

`lib/index.js`（形态常量与渲染 / `normalizeTeams` 降级留痕 / 宿主探测 `probeAgentTeams` / 确认框 `askModeDialog` / `set-mode` 分支 / `roster get` 与 `team_link_status` 的形态段 / 两个工具的描述面）；`host-half.test.mjs`（41 条断言 ＋ 宿主 `agentTeams` 替身 ＋ `rosterCall` 的 Y7-safe 调用器 ＋ roster 参数键集锁）；`README.md`（三处读数 · roster 动词表与工具一览 · **新增「团队形态」段**（两档白话 + 三句要紧的话 + 能力矩阵）· 状态卡 ⑦ 段 · 设置键表与两处 yaml 样例 · 设计文档索引加本档一行）；本条目；`docs/verification-log.md`（本批一节）；`docs/team-mode-batch-design-2026-09-26.md`（状态行与 §8 假设 A1–A4 回填实测结论）；`docs/team-ledger-and-mode-design-2026-09-26.md`（§7 步 3 / 5 标 ✅ ＋ 指向本档的链接；提交哈希 `12f0874`（2026-09-27 由父会话回填））；`docs/observability-batch-design-2026-09-26.md`（§7 第 6 条改标「状态卡形态段由 B 批实施」）。

---

## 未发布 — 分歧审计修复轮 `DIVERGENCE(12)`（2026-09-27）

> 只读分歧审计的 **12 条**结论（D1–D12）逐条闭环。**不夹带新功能、不扩范围**：A 批设计 §7「明确不做」继续有效，B 档 `docs/team-mode-batch-design-2026-09-26.md` 一字未动。逐条读数见 `docs/verification-log.md` 的「分歧审计修复轮」一节。

### ✨ 修了什么

**① 唯一的行为改动：派生回执行标注「按当前名册解析」（D7）** —— 没有结构化回执时按**实参**兜底解析寻址表达式，其中 `team:<name>/<role>` / `team:<name>/*` 是**按读取时的名册**解析出来的；此前注释承诺了标注而实现没做（只特判了「回执截断」），换届之后可能把 ✅/⚠ 判给当时并非收件人的会话且**不告知**。现在这类行如实带上 ` · 按当前名册解析（该表达式按读取时的名册解析，未必是投递当时那一个）`（裸 id 是字面量，不带此标注），工具描述面同批同步。

**② 10 条断言补齐（D1 / D2 / D4 / D5 / D9 / D10 / D12）**：

1. **D1**：无时间戳 ⇒ 打「无法计算」那句、**不打**计数注记 —— 设计 §8.1 A5 与本档都曾写「三态都有断言」，而套件里 `grep 无法计算` = **0 命中**（补的是判据，不是行为）；
2. **D2**：读窗为空 ⇒ 注记**整段不打印**（连标题都不出现）；
3. **D4**（四条）：`run_code` 里发出的 `tool/ptc-dispatch` 那一类投递照样成行 · 实参兜底解析 `team:<name>/<role>` 时表达式与现任一并显示 · 回执被裁过时只报告**回执自己报的总数** · 逐行渲染上限恰 20 行 ＋ 一行「本次还有 N 行未显示」（N = 真实差额）；
4. **D5**：`team_read` 的 `readIds` **非数组** ⇒ 拒绝且**零读**（工具面硬形状）；
5. **D9**：`U13 零额外读` 那条原先有半边**恒真**（`slice(length).length === 0`），换成真计数 —— 打注记那次与不打注记那次，各自的 surface 读数逐次相等（各恰 12 = 同一个有界读窗）；
6. **D10**：聚合态 `M` = **所示各团队** `tasks.md` 同期新增行数之**和**（夹具 3 ＋ 2 = 5，两个团队的台账尾都在卡上）；
7. **D12**：归组面 = **任何消息文本里第一个** `ref=t-<n>` —— **非 banner** 的普通消息（本会话自己发的那条）同样归组，发送方如实回落标「（本会话）」。

**③ 设计与 README 口径回填（D1–D4 / D6 / D8 / D10–D12）** —— 设计档 §4.1 新增「点名读上限与超限行为」表（`list_sessions` ≤12 拒绝 / `team_read` ≤11 截断并逐个标注，同一不变量两种形态）· §4.2 注记补**三态**与**聚合口径**、成本列回填「单团队 1 次 / 聚合 N 次（N = 所示团队数）」、会话面来源订正为 `ctx.sessionQuery.listSessions`（与 `list_sessions` **同源同序**）· §4.3 写死**归组面**与点名读上限 · §4.4 写死投递事实的**两类事件载体**（`tool/call` ＋ `tool/ptc-dispatch`）、**`callId` 配对**、**窗口单位是「最近 20 次 `team_link_send`」而不是 20 条消息**，并补**第 4 态**「：未投递（outcome=…）」（措辞只陈述 `outcome`，不下结论）；README 同步这些口径，并修正设计文档索引里与父档矛盾的状态行（步 2 / 4 / 6 已实施；步 3 / 5 待实施）。账本 A 批一节 A5 行的「都有断言」也已订正。

### 💡 为什么

这一批的共性是**读面与描述面脱节**：行为大多已经存在，但（a）有两条断言是**空的**（一条恒真、一条根本不存在），(b) 有若干实现口径**没写进设计**（无锚时的三态、聚合口径、`team_read` 的读额边界、投递事实的载体与窗口、第 4 态、归组面、成本列），(c) 有一处注释**承诺了没实现的标记**（D7）——那才是唯一真会让人读错数的缺陷：换届之后按当前名册解析出来的收件人，会被当成投递当时的收件人来读。处置原则沿用本仓两条家规：**能标注的就不沉默**（无法计算 / 本次未读 / 按当前名册解析），**能红的断言不许恒真**。

### 🚫 明确不做（本轮）

不加新工具 / 新参数 / 新事件类型 / 新持久状态；不改 `list_sessions`、`team_link_status`、`team_link_send`、`team_link_team_append` 的任何行为；不动 `PREVIEW_SESSIONS`、不动三个 `timeoutMs`；B 档（形态）一字不碰。

### 🧪 怎么验证

- **命令**：`node host-half.test.mjs && node client-half.test.mjs`；
- **绿相（实跑两行原文）**：`node host-half.test.mjs` → `ALL PASS / assertion total: 1127 (failed: 0)`（宿主进程退出码 0）；`node client-half.test.mjs` → `ALL PASS / assertion total: 269 (failed: 0)`。基线 `1117` / `269` ⇒ **新增 10 条宿主断言**（`1127 = 1117 + 10`），浏览器半边**一字未改**；
- **红相 A（「本轮修复前」的实现 = 工作树、D7 尚未落码）**：`assertion total: 1127 (failed: 1)` —— 唯一红的那条就是 **D7**；其余 9 条在两种实现上都绿，**如实标为负相**（它们补的是判据覆盖缺口：旧实现没有这个 bug，只是没有这条锁）；
- **红相 B（A 批实施前的 `89074fe`）**：`assertion total: 1127 (failed: 65)` = A 批的 **55** 条（54 新 ＋ 1 既有工具计数锁，与账本的 A 批一节逐条相符）＋ **本轮 10 条全红**；安全方法同 A 批：工作树版读进内存 → `git show HEAD:lib/index.js` 覆写 → 跑 → **还原内存里的那份** → **sha256 逐字节复核**（`restoreMatch: true`，本轮工作树版 `d6ae2f78c1d6c13db8fb2d8ad0cc56cfd623702f7a675675d69c074211b9ab1a`），**不留备份 / 探针 / 残留**；
- **回归**：A 批 60 条断言全部仍绿（含 `U12` 既有四块零回归、`U7` 三条成本不变量、`§12.5` 跨半边 outcome 短语锁、工具计数锁 10），模块级 `inject` 仍 4 项，`policy` 命名空间键集不变。

### 🔁 同批同步的面

`lib/index.js`（`ROSTER_RESOLVED_NOTE` ＋ `receiptRowsOf` / `renderReceiptRow` 的标注 ＋ `team_link_team_read` 描述面）；`host-half.test.mjs`（10 条断言 ＋ 4 条夹具事件 ＋ 3 个新夹具 ＋ `trTruncatedResult` ＋ `U13 零额外读` 改真计数）；`README.md`（三处读数 ＋ 状态卡段 ＋ 收件视图/派生回执段 ＋ 回执四态表 ＋ 设计文档索引状态行）；本条目；`docs/verification-log.md`（本轮一节）；`docs/observability-batch-design-2026-09-26.md`（§4.1–§4.4 口径回填 ＋ §8.1 A5 订正 ＋ 状态行加「后续回填」）。**`docs/team-ledger-and-mode-design-2026-09-26.md` 与 B 档一字未动。**

---
## 未发布 — 可观测批（A 批：读窗扩展 · 团队状态卡 · 台账收件视图 ＋ 派生回执）

> 设计（唯一事实源）：`docs/observability-batch-design-2026-09-26.md`（§4.1 读窗 / §4.2 状态卡 / §4.3 收件视图 / §4.4 派生回执 / §5 红线五条 / §6 判据 **U1–U14** / §7 明确不做 / §8 假设 A1–A5）；来源是《台账 + 形态设计》§7 的**步 2 / 步 4 / 步 6**（步 6 的两半都在本批）。逐条红相/绿相读数见 `docs/verification-log.md` 的「可观测批（A 批）」一节。

### ✨ 修了什么

**① 读窗从「固定 12」变成「默认 12 ＋ 点名 ＋ 分页」（§4.1）** —— `team_link_list_sessions` 新增两个**可选且互斥**的参数：

1. `readIds?: string[]`（点名读）：去重后 ≤ **12** 个，超出即**拒绝且本次零读**；只接受**本次列表内**的 id，未知 / 不属于本列表的 id → 拒绝并**指出那个 id**；点名**优先占额**，其余按原顺序补足到 12；本就在默认窗内的 id 只占一个额、不重复读；与 `offset` 互斥；
2. `offset?: number`（分页）：从第 `offset+1` 行起读 12 行（默认 0）；非整数 / 负数 / 越界 → 拒绝并给出**有效区间** `0..行数-1`；
3. **成本不变量按工具分账**（断言锁死）：`list_sessions` 三条路径 ≤ **12**；`team_read` ≤ `min(12, 1+|readIds|)`（**自读占 1 个额**）；`team_link_status` ≤ **12**；
4. **列表末尾新增「未读 Y 行 ＋ 怎么读」提示段**（默认调用也带）：`未读 2 行（读窗 12/共 14 行）：读第 13–24 行 → offset=12；或点名 → readIds=["session-x", "session-y"]`。**既有行结构一字未改**——本次唯一的格式新增就是这一段（外加新参数）。**默认值 12 不动**（提高它等于把超时风险塞回每一个调用）。

**② 新工具 `team_link_status`：只读团队状态卡（§4.2）** —— 一次调用一屏，六段：① 团队与角色（在位 / 空缺 / **版本史末条**）② 换届 pending（在飞令牌，token **掩码**）③ 看门狗（`policy.watchdogs`）④ 会话面（id / 代理状态 / 创建时间 / provisional 标记，**不读日志**）⑤ 活性（**有界**：与列表工具同一顺序读前 12 行，超出如实标「本卡只读了前 12 行」）⑥ 台账尾（`tasksTail` 默认 **5**、上限 **20**，超出或非正 → 拒绝并给区间）。参数：`team?`（省略 = 聚合全部团队、每团队一段；给了但不存在 → 拒绝并**列出已知团队名**）、`tasksTail?`。**全只读**：不写设置、不写磁盘、不加缓存、**连换届过期清扫都不跑**（那会写）；无会话身份时照常出卡，「当前会话」处如实标 `（当前会话未知）`。**反面预警注记**（会诊提出）：锚 `T0` ＝ 本读窗内最旧的一条 surface 消息时间戳、`N` ＝ 其后本读窗内的消息条数、`M` ＝ 其后 `tasks.md` 的新增行数，**只有 `N == 0 且 M > 0` 才打注记**（「会话可能已停止说话只写行」），否则不打；两个计数都取自**已经读到**的数据 ⇒ **零额外读**；锚取不到时如实标「无法计算」，**不猜**；**它是读数，不是裁决**。

**③ 台账收件视图 ＋ 派生回执（§4.3 / §4.4）** —— `team_link_team_read` 新增**一块**（既有四块一字未动）：

- **收件视图**：读**调用方自己**的会话面，按 `ref=t-<n>` 分组给出「谁 · 什么时候 · 开头几个字符」；两个尺寸常量 `TASK_INBOX_MESSAGES=20` / `TASK_INBOX_PREVIEW=80`（越界截断**必标注**）；无 `ref` 的消息**如实计数**（不静默丢弃），**未登记的任务号照样分组并标注** `（该号在台账里未登记 —— 可能是笔误，也可能是先发后记）`；
- **派生回执**：投递事实从调用方**自己**会话面的既有事件取（`tool/call`，或 `run_code` 里发出的 `tool/ptc-dispatch`；有结构化回执时用**投递当时**解析出的目标与结果），三态写死：**未点名** ⇒ `未读`（**目标面零读**，断言锁死）／点名且 `t` 之后有 assistant 事件 ⇒ `✅ 有反应`／点名且其后无 assistant 事件 ⇒ `⚠ 无后续反应`；读不到的面一律标「未读」，**绝不用空数据算读数**；`≤ min(12, 1+|readIds|)`，超额的点名逐个标「本次未读」。**纯派生、零新增持久状态、零新增写路径**。

### 💡 为什么

「12」是**预算**（一行 surface ＝ 一次冷日志解压 ＋ 一次投影；真实工作区里串行读满 50 行会超掉 60s 工具预算），不是能力上限——所以**不提高默认值**，把「读哪些」的决定权交回调用方（点名 / 分页），并把上限定成不变量；同时把「未读」从终点变成**可执行的下一步**。另外两件事都是**只读地**把已经存在的事实摆到一屏里：状态卡回答「人想知道团队还活着没有」，收件视图回答「谁对我说了什么」，派生回执回答「我说出去之后对方动了没有」——后者是**纯派生**，不新增任何持久状态（会诊 Q4 采纳项）。**红线一寸未动**：成本不变量按工具分账、未读必标注、**全只读**（`send` / `team_append` / `roster` 的写面一行未动）、`list_sessions` 既有行结构逐字保持、措辞不出现「状态＝完成」这类把主张写成事实的词。

### 🚫 明确不做（本批，§7）

不做客户端面板渲染（本批只做数据面）· 不做缓存 / 索引 / 自动后台巡检 · 不做跨进程聚合 · **不提高 `PREVIEW_SESSIONS` 默认值** · 不定 agent-team 档下的状态卡行为（形态字段尚不存在，跨批接缝显式声明）。

### 🧪 怎么验证

- **命令**：`node host-half.test.mjs && node client-half.test.mjs`；
- **新增断言 60 条**（全部落在宿主套件；读数 `1117 = 1057 + 60`，基线取本批前的套件自报值 1057）：阶段 1（读窗 / 成本不变量 / 尾巴提示 / 三类边界拒绝）23 条 · 阶段 2（状态卡六段 + 参数边界 + U13 注记）20 条 · 阶段 3（收件视图 / 派生回执 / U12 回归）17 条；
- **两套件实跑自报读数**：`node host-half.test.mjs` → `assertion total: 1117 (failed: 0)`；`node client-half.test.mjs` → `assertion total: 269 (failed: 0)`（浏览器半边本批**未触碰**：本批只做数据面）；
- **红相（安全方法：备份 → 换实现 → 跑 → 还原 → sha256 复核）**：把 `lib/index.js` 换成 `git show HEAD:lib/index.js`（`89074fe`）后跑本批的测试 —— `assertion total: 1117 (failed: 55)`，其中**本批新断言 54 条红**（其余 6 条是刻意在两种实现上都应通过的**回归锁**：U1 的三条「行结构 / 读窗 / 窗口内 verdict 不变」、U12 的「派生读数与原始行窗口未动」、U3 的「本就在默认窗内的 id 不重复读」、以及 U9 的 settings 半边在红相上 trivially 成立——旧实现根本没有这个工具，自然不改设置），另 1 条是既有的 `§11.9 工具计数同改锁`（在那个临时状态下 README 写 10 个工具而旧实现注册 9 个——这正是它该红的原因）。**还原后 sha256 逐字节一致**（`fde1b78573f88d392cdf3666c38bf1f70132439209666d590bfc46bf546ff7c1`），工作树无残留；
- **回归锁**：`list_sessions` 的既有行结构 / 既有断言零回归；`team_read` 的**既有四块**（roster ＋ decisions ＋ discipline ＋ tasks）零回归，新块只追加；
- **红线回归**：模块级 `inject` 仍 4 项、**不新增任何日志事件类型**（新读全部走既有事件：`tool/call` / `tool/ptc-dispatch` / 既有 `tool/result.meta`）、`policy` 命名空间**键集不变**（零新增持久状态）、投递 `source` 仍恰三成员、`§12.5` 跨半边 outcome 短语锁与 README 工具计数锁同批更新后仍绿。

### 🔁 同批同步的面

`lib/index.js`（`readListReadWindow` / `listReadTail` / `readStatusTasksTail` / `ledgerRowTime` / `reverseWarningNote` / `readTaskReceiptTargets` / `surfaceMessageEvents` / `surfaceEventText` / `shortStamp` / `crossSessionBody` / `inboxOpening` / `readReceiptTargets` / `parseToolArguments` / `surfaceSendRecords` / `resolveSendTargets` / `taskMessageBlock` / `receiptRowsOf` / `renderReceiptRow` / `asText` 与两组常量；`team_link_list_sessions` 的参数面与尾巴、`team_link_status` 新工具、`team_link_team_read` 的 `readIds` 与新区块）；`host-half.test.mjs`（A 批 60 条断言 ＋ `listCall` / `statusCall` / `readCall` 三个 Y7-safe 调用器 ＋ `mkdir` 进导入 ＋ 工具计数 9 → 10）；`README.md`（§二 读窗参数与成本不变量表 · 新增「团队状态卡（只读）」一段 · §四 收件视图与派生回执 · 工具一览表两行 ＋ 新工具一行 · 设计文档索引 ＋ 目录 · 三处读数）；本条目；`docs/verification-log.md`（本节读数）；`docs/observability-batch-design-2026-09-26.md`（§8 假设 A1–A5 的实测结论 ＋ 状态行）；`docs/team-ledger-and-mode-design-2026-09-26.md`（§7 的步 2 / 4 / 6 标已实施并链接本档）。

---

## 未发布 — 团队任务台账 `tasks.md`（设计 §9 第一批：只追加台账 ＋ 读时投影）

> 设计（唯一事实源）：`docs/team-ledger-and-mode-design-2026-09-26.md` **§9**（§9.3.1 行格式 / §9.3.2 写入面 / §9.3.3 读面 / §9.3.4 派生规则 / §9.4 边界与红线 / §9.5 判据 **U1–U18** / §9.6 明确不做 / §9.9 跨文档同步六条）。

### ✨ 修了什么

**新增第三个黑板文件 `tasks.md`**（`<workspace>/team/<name>/tasks.md`）——只追加的任务台账，与 `decisions.md` 同构：只追加、每行带署名、seq 由插件单调分配、单行 500 码点。

1. **行格式（§9.3.1）**：`seq | ISO时间 | author-session-id | kind | task | 正文`。`kind` 是**闭集**：`plan` / `claim` / `done` / `block` / `dispute` / `retract`；`task` 形如 `t-<n>`。
2. **写入面（§9.3.2）**：`team_link_team_append` 的 `file` 白名单由两值扩为**三值**，新增 `kind` / `task` 两个参数。`kind=plan` 由插件**扫整个文件**分配 `t-<已登记最大号+1>`（调用方同时给 `task` 即拒绝）；其余 `kind` 必填 `task`，且**该号必须已在本文件登记过**——未登记的号一律拒绝并指路 `kind=plan`；`kind` / `task` 传给 `decisions` / `discipline` **一律拒绝且零写入**；`seq` **每文件独立计数**；写入走 `appendFile`（读-算 seq-追加），只追加不重写。
3. **读面（§9.3.3）**：`team_link_team_read` 在 discipline 块之后追加 tasks 块 ＝ **原始窗口**（末 20 条，逐字）＋ **派生视图**（扫描最近 500 行，逐任务给「最后主张」与「未消解存疑」）＋ `baseHash`（仅供参考/审计：tasks 只追加、不接受 baseHash 参数）＋ **三类边界如实标注**（文件不存在 / 超扫描上限「更早的 N 行未参与派生」/ 不可解析行「N 行无法解析，未参与派生」）。
4. **派生规则（§9.3.4）**：最后主张 ＝ 该任务最后一条 `claim` / `done` / `block`，一条都没有 → 「尚无主张（仅登记）」；`retract` **不抹历史**——被撤回的主张仍然显示（呈现「最后主张 done（… · 已由 #N 撤回 · 撤回者 …）」，**原主张者与撤回者两个 id 都在**）；`dispute` 之后**没有**新的主张 / 撤回才标 `⚠ 未消解存疑 N 条`，否则标「存疑已由 #N 消解」；**绝不自动裁决**（不比较正文、不按时间戳定胜负），措辞一律「最后主张 / 未消解存疑」，**不出现**「状态＝完成」这类把主张写成事实的词。

### 💡 为什么

主会话的上下文会被压缩，而「我们到底在干什么」过去只活在它脑子里；但**报告必须原话进上下文**这条红线又不能动（把报告换成「写张便条贴墙上、让主管自己看」，互核就死了）。台账补的正是中间那一格：**把「账」搬出上下文，把「事」留在上下文**，并且**在结构上无法成为事实源**——

- **只追加** ⇒ 不存在「谁有资格把 doing 改成 done」这个无仲裁者的问题（不抄 CAS 看板：那张板子上写着 done 就是「现实」，一个错被全员继承）；
- **每行带署名** ⇒ 板上永远只有「A **声称** T3 完成」，没有「T3 完成」；
- **反驳走同一条写入路径** ⇒ 存疑只是**又一行**，不需要任何新机制，也不需要新的授权类型；
- **「当前状态」是读的时候算出来的** ⇒ 显式标注「这是派生读数，不是事实」；删掉文件不影响任何写入路径，派生结果随时可从原始行重算。

### 🚫 明确不做（本批，§9.6）

不做状态机 / CAS / 自动裁决 / 自动推进（「送达即完成」禁止）· 不做写入权限门 · 不做跨会话物理队列 · 不做 12 窗口 / 形态切换 / 状态卡 / 交卷提示（§7 第 2–5 步不在本批）。**`decisions` / `discipline` 的语义与文案一字未改**，唯一例外是 `file` 白名单拒绝文案由两值改三值（§9.6⑤，已用**源码级**断言锁住）。

### 🧪 怎么验证

- **命令**：`node host-half.test.mjs && node client-half.test.mjs`；
- **新增断言 62 条**（全部落在宿主套件 `host-half.test.mjs`；U1–U18 逐条对应，读数 `1057 = 995 + 62`，基线取本批前的套件自报值 995）：**51 条**随第一批（台账本体）、**11 条**随同日分歧修复轮（D4 判据缺口 5 条：U4 的 `=500 码点收` · U12 的 `block 可作最后主张` · U18 的另两组组合与四组合总锁 3 条 · D5 1 条 · D6 2 条 · D9 2 条 · D1/D8 的描述面口径 1 条）。**两套件实跑自报读数**：`node host-half.test.mjs` → `assertion total: 1057 (failed: 0)`；`node client-half.test.mjs` → `assertion total: 269 (failed: 0)`。客户端半边本批**未触碰**；
- **红相（功能缺失，两次实跑）**：
  - **① 本批判据 ＋ 旧实现（`3eba6c1:lib/index.js`）**：`node host-half.test.mjs` **在 `host-half.test.mjs:2145` 当场抛错、整轮中止** —— `ToolArgsError: invalid arguments: "file" must be one of ["decisions","discipline"]`（`code: 'INVALID_ARGS'`），`nodeExit=1`，**`assertion total` 一行都没打印**（红相说不清自己的规模，与本仓 Y7 教训同形）。原因正是旧实现的 `file` 枚举里没有 `tasks`、`kind` / `task` 也还不是已声明参数（新判据打旧实现会**抛**，而不是返回 FAIL）；
  - **② 本批判据 ＋ 本轮修复前的 `lib/index.js`**（把 D1 / D5 / D8 / D9 四处改动逐字回退）：`assertion total: 1057 (failed: 8)`，8 条逐条点名 —— D9 1（`t-1e+22` 那条分配路径）· U13 三条**全行比对**（t-2 / t-4 / t-1，旧实现渲染成 `） · ⚠ …`）· D1/D8 的读面口径行 1 · D6①/② 2 · D5 1。U4 / U12 / U18 的 5 条在修复前**本来就绿**（补的是判据覆盖缺口，如实记、不算红相）；
  - **逐条读数、夹具与还原纪律见 [`docs/verification-log.md`](docs/verification-log.md) 的「团队任务台账 `tasks.md` 第一批（设计 §9）＋ 分歧修复轮」一节**；
- **U14 是回归锁**：`decisions` / `discipline` 的**整段返回文本**用 `===` 逐字比对（不是 `includes`），任何一处文案漂移当场变红；
- **红线回归**：导入面仍**六模块**（零新增依赖）、**不新增任何日志事件类型**、投递 `source` 仍**恰三成员**、`policy` 命名空间键集不变——本批只读写磁盘上的黑板文件，不碰投递层、不碰 settings 形状、不引入任何内存态。

### 🔁 同批同步的面

`lib/index.js`（常量 `TASKS_FILE` / `TASKS_WINDOW` / `TASKS_PROJECTION_SCAN` / `TASK_KINDS`、`blackboardPaths` 的 `tasks` 键、公共 `lastSeq`、`tasksRow` / `parseTasksRow` / `registeredTaskIds` / `deriveTasksView` / `taskView` / `taskStamp` / `renderTaskView`、`team_link_team_read` 与 `team_link_team_append` 的两个面；分歧修复轮的 D1 分隔符 / D5 删未申报段 / D8 描述面三件套口径 / D9 安全整数分配）；`host-half.test.mjs`（§9 的 **62 条**断言（51 ＋ 修复轮 11）＋ `tasksPath` / `tasksBlockOf` / `tasksHashOf` 三个夹具助手 ＋ 那条「三个 baseHash」既有断言的同批更新）；`README.md`（§四 目录树三件套 / 参数说明 / 任务台账小节 / 台账推荐用法 / 工具一览表 / **三处读数**（徽章 · 对照表 · §十））；`docs/verification-log.md`（本节读数：本批做了什么 / 红相两次实测 / 绿相 / 边界证据）；本条目；`docs/team-ledger-and-mode-design-2026-09-26.md`（§7 第 1 步与 §9 状态行标已实施并附实测读数；§1 / §4 的 `conflicted` 口径改成已实施的措辞；§9.3.4 规则 2 补两条子情形定义）。

---

---

## 0.3.10 — 2026-09-22（当前版本 · **发布收口：0.3.9 ＋ 0.3.10 一并落定**）（§10.2.8 `/team_session` 三条真机缺陷修订：输入文法 · 结果可见性 · 确认框边界）

> 设计（唯一事实源）：`docs/collab-enhancements-design-2026-09-19.md` **§10.2.8**（§10.2.8.0 现象与证据 / §10.2.8.2 修订后的文法 / §10.2.8.3 结果可见性 / §10.2.8.4 确认框边界 / §10.2.8.6 明确不做）与 **§10.4 的 U30–U34**；逐条**红相/绿相读数**见 `docs/verification-log.md` 的「§10.2.8 三条真机缺陷修订（U30–U34）」一节。
> **发布状态（2026-09-22 发布收口）**：本条目与 **0.3.9** 一并落定为当前版本（`package.json` **0.3.10** · tag **v0.3.10**；0.3.9 段按本仓惯例标注「发布前收口于 0.3.10」）。①② 是宿主半边（需 DSH 重启才在真机生效）；③ 含**一号宿主半边**（换槽位）与**二号浏览器半边**（§10.2.8.3 通道 1 的客户端节点，需重建客户端 bundle + 刷新页面）。
> **真机状态（取代原先那句「本条目不声称已真机验证」）**：§10.2.8 的缺陷 ①②③ 与 §10.2.8.9 / §10.2.8.10 的四项裁定**均已真机验过** —— 含确认框在真机上的四条判据、通道 4 回执（GUI 渲染为「跨会话消息」卡片）、最小唤醒、侧边栏三个会话可见、结算时序读数；逐条读数与截图证据见 `docs/verification-log.md` 的「真机复验」一节。**U33 的「渲染高度不超视口」**这一条也已被动验到（框体紧凑、底部动作行全在视野内），但它依赖窗口宽度，**仍不作单测断言**。

### 🐞 三条真机缺陷与修法

**① 输入文法：正文被当成了参数（U30 / U31）**。真机现象：用 `/team_session team=0921-main ` 接一具 **3058 字**的交接正文，连发 5 次，界面「点了发送什么都不显示」、团队与会话一个都没建成；会话日志里是 **5 条 `command/run` + 5 条 `command/done kind=error`**，错误文案逐字相同：`N 必须是整数（会话个数，1..8）。`。**根因**：解析器在**整条输入**上做 token 扫描，**任何**「字母＋等号」都被当成 `key=value` —— 正文里的「同窗 `N=3）。`」被读成 `n=3）。` ⇒ **参数校验阶段即拒绝**，连「角色名」那一步都没走到。同族的第二个雷：正文里还有 **284 个**不含等号的 token，按当时文法**全是位置角色名** ⇒ 即便躲过 `N`，也会以「会话个数 284 超过硬顶 8」失败。⇒ **当时没有任何一种拼法，能把一具含「字母＋等号」的正文送进去**（`task=` 也会在第一个已知 `key=` 处断开）。

**修法（用户裁定 = 方案 A「参数可省」，§10.2.8.2 的 R1/R2/R3）**：**R1** 参数区**只在行首**且谓词闭合 —— 从行首连续取「形状合法（含 `=` 且 label 非空）、key 已知、**取值本地合法**」的 token 为参数；结束条件**恰两类**：**(a) 静默结束**（token 不含 `=`，或 `=` 在首字符 ⇒ 该 token 起全部算正文）· **(b) 报错**（形状合法而 key 未知，或 key 已知而取值不合法 ⇒ **整条命令拒绝**并**原样回显 offender**）。**R2** 正文不解析：进入正文后 `N=3` / `pid=384448` / `word=` 一律**逐字保留**。**R3** 失败必须点名：回显 offender + 给出路「想写正文就直接写…也可以用 `task=`」。**位置角色名废除**（本次的语义变更，已在设计、命令 description、`input.hint` 与报错文案里**显式宣布**）：裸 token 是**正文的起点**，声明角色的唯一拼法是 `roles=`（别名 `role=`）；旧的「位置参数写角色名」指引同批删除。**默认值**随之落定：`team` 省略 ⇒ 调用会话工作区目录名（不合 `[a-z0-9-]+` 回退 `default`）· `n` 省略 ⇒ 1（给了 `roles=` 时为角色数）· `roles` 省略 ⇒ 一个 `worker-1` · 既无正文也无 `task=` ⇒ 只建会话、不投启动任务。**既有语义一字不改**：N ≤ 8、每队 ≤ 24、按 role 幂等、两个别名、`model`/`preset` 规则、确认框、失败即停。

**② 结果可见性：命令结束了，界面上一行都没有（U32）**。真机现象：那 5 条错误**逐字写进了会话日志，界面上一行都没显示** —— `command/run` / `command/done` 是 **log-only** 事件（**不进模型面、也不保证在壳里渲染**），落点只在发命令的那个输入框里（§10.2.1 那条「顶层可见是免费的」被真机推翻）。**修法**：按 §10.2.8.3 的通道优先级取**可用的第一条** —— **通道 1（复用 §10.1.3 的 D 模式）经核实可用，即取通道 1，不降级**：客户端新增**独立 definition**（`kind: team-link-team-session`）与同一键控槽上的渲染面，match 既有 `command/run`（**按 `data.name` 过滤**）与 `command/done`（该事件**本身不带 name**，只能按**见过的 `commandId`** 认领 —— 有界 64、重放安全），**成功与失败走同一条通道**。**不新增任何日志事件类型**（只**读**既有事件）。

**③ 确认框无界、动作够不着（U33）**。真机现象：确认框终于弹出，但**框体高于可视区，底部的「创建 / 取消」够不着**（那次成功命令的 **27.4 秒全花在确认框上**；此前另有 **8 次** `kind=success` 的失败尝试走的是「取消则零创建」那一支）。**先修正归因（有代码依据）**：撑高框体的**不是** `task=`（那行早就是有界预览），而是**我们自己写的散文**。**修法（修法表第 0 行，首要、结构性）——换槽位**：披露正文从 `question` **整段移入 `detail`**，`question` 只留**一行话**。依据是壳的布局（逐条可复核）：`question` 被渲进**无高度钳制、且在滚动容器之外**的 `<header><h2>`，而 `detail` 在 `Mbwy4a_body{overflow-y:auto}` **之内**、底栏 `flex-shrink:0` 也在滚动区外 ⇒ **动作可达性与文本长度脱钩**。同批：任务预览**标注**（共 N 字 / 仅显示前 M 字 / **完整正文会原样投递**）· 正文两个硬预算（**码点 ≤ 600 且换行 ≤ 12**，两者都单测断言）· **不逐角色展开**（只列前 3 个 + 「…等 N 个」，**总数仍写全**）· **「只给 id 形状 ＋ 计数」**（`- 会话 id：team-link-<team>-<role>-<uuid8>（共 N 个…）` —— 修法表第 0 行换槽位时把这行连同逐角色 id 清单一起删了，§10.2.8.4 (b) 要求的这半句一度零实现；全量清单仍只出现在完成回报里）· DEFECT-4 与模型/预设两段自述各压到**一行** · 可变字段（团队名 / cwd / 会话 id / **model= / provider=**）逐字段封顶且裁剪留下省略号 · 按段落裁剪时**标注**（「已裁剪至 600 码点 / 12 行上限，省略 N 段自述；必备披露一字未少」）。§10.2.8.7 裁定 2 的**角色指引行**（「新会话是 worker；你自己（调用会话）是 coordinator；要交棒请用 `team_link_rotate`」）在**确认框与完成回报**里各一行，并**计入**上述预算。

**明确不做**（§10.2.8.6 / §10.2.8.4 (d)）：**不驱动调用会话**（`followup` 会花 token，且违背命令的 log-only 语义）· **不加**「建完即换座」（会绕开 M4 的信任快照 / 冻结广播 / 对称吊销 / 一次性令牌两阶段）· **分页杠杆已核实不适用**（壳的分页是**按问题**且每页须作答，拆散文不行）· **不改**模块级 `inject`（仍 4 项）· **不改**既有 schema key · **不新增任何日志事件类型**。

### 🧪 怎么验证（变异验证）

- **修复前必红**（隔离夹具 `.tmp-head/`：本轮的新测试 + `git show HEAD:lib/{index,client}.js` + HEAD 的 `README.md` / `package.json`）：`node host-half.test.mjs` → `assertion total: 951 (failed: 30)`；`node client-half.test.mjs` → `assertion total: 269 (failed: 22)`。
  - 宿主侧 30 条的**实际分布**（**逐条点名**，同夹具实跑）：**U30 7/9 · U31 4/4 · U33 8/9** ＋ 改写后的既有断言 **U16 10 条** ＋ **DEFECT-4 槽位 1 条** = 7 + 4 + 8 + 10 + 1 = **30** ✓。**其中 3 条在旧实现上本来就绿**（原记「本批新增的 U30 / U31 / U33 全红」是**计数不实**，2026-09-22 只读审计第 6 条纠正）：**U30 的 R1(b) 正相**（`n=abc` ⇒ 报错 —— 它断言的是**保留下来**的旧语义，不是本批引入的新语义）· **U30 的「既有 key 语义不变」** · **U33 的 `options` 一字未动**（换槽位只搬正文，不碰「创建 / 取消」这对授权面）。其中 U33「换槽位」那两条把**旧的槽位读数逐字留在案上**：修复前 `question` = **1068 码点 / 14 换行**（正是「长文放在 header」的形状），而 `detail` 只有 **63 码点的一行话** —— 换槽位把这两个数整个对调了。
  - 客户端侧 22 条里，**U32 的 10 条中 8 条红**；另 2 条是**负相**断言（「不认别的类型」「状态不可读时不抛错」）—— 它们断言的是**缺席**，在没有该 definition 的构建上**本来就会绿**，如实记，**不算**「修复前已通过」。
- **修复后全绿**：`node host-half.test.mjs` → `assertion total: 951 (failed: 0)`；`node client-half.test.mjs` → `assertion total: 269 (failed: 0)`。新增断言 **24 条宿主 ＋ 10 条客户端**（`951 − 927 = 24`、`269 − 259 = 10`，基线取 0.3.9 批次 4 收口时的两套件自报值，**不按「上一版 ± 本轮新增」反推**）。
- **U34 是回归守卫（§10.2.8.6）**：`/team_rotate` 的位置参数（角色名）**改前改后都应为绿** —— 隔离夹具里两条 U34 断言**当场全绿**（与 §11.2 的既有文法面同批复跑）。**红相另做单点变异**「把新文法搬到 `/team_rotate` 的解析器上」（`if (value.role !== undefined) continue;`）⇒ 套件在 `host-half.test.mjs:4646`（§11.2 的既有文法面）**当场崩**、整轮 30 条 FAIL —— **守卫是活的**。
- **红线回归**：`lib/` 内 `ctx.session`（**单数**）/ `appendEvent` / `session.log` / `.append(` **仍全部为 0**；模块级 `inject` **仍恰 4 项**（`sessionReferenceResolver / tools / sessionQuery / agents`）；投递门与 `source` 三成员语义未动；**未新增任何日志事件类型**。
- **同批改写（不是删除）的既有断言**：
  - 宿主侧 —— `input.hint` 那条（补「不得再有位置参数写角色名」）· 三条**位置角色名**断言（§10.2.8.2 的语义变更，改为「裸 token 归正文、`roles` 只由 `roles=` 声明」）· 「U16 端到端文法」那一组（输入行在新文法下读法不同，两条改写成 U30 的判据、一条改为 `roles=` 声明）· 以及 **5 处披露文案断言只把取值槽从 `question` 换成 `detail`**（**断言文本逐字不动** —— 换槽位把正文搬进了 `detail`，断言若还读 `question` 就是在测一个已经不承载正文的槽位）。
  - 客户端侧 —— 3 条 chat-node 槽位计数（2→3，新增 §10.2.8.3 通道 1 的渲染面）· 1 条「注册被拒只花顶层卡」的 warn 计数（1→2，两个 definition 各自留一行具名 console warn）· 若干 `fresh.length`（5→6）与 `names()` 清单 · `applyWith` 的 definition 计数改成**按 kind**（新增 `definitionCallsOf(kind)`，让「某一张面照常注册」的断言不再被另一张面合法地移动）。

### 📝 如实标注（设计未逐字规定 / 与实现现状有出入的地方）

- **原来那句「本命令是 §10.2.4 的批量动作：…」被**替换**，不是被删掉**：按修法表第 0 行，`detail` 现在整段承载披露正文；该句描述的批量范围由**必备披露块**「确认则：创建 → followup 投递启动任务 → 登记 roster 与 pairs；取消则零创建、零 pairs。」承接。**为什么两全不了（实测）**：正文预算在常规用例下已是 **591 / 600 码点**（另有 9 / 12 换行；**2026-09-22 修正批**把 id 形状行补回之后由 578 升到 591，见本条目末节），任何**必备**块的增加都会挤掉可选的自述块 —— 实测会先掉 DEFECT-4 的标题段，进而让「确认框说明设了什么标题」那条既有断言变红。
- **`question` 未加 `intent: plan-review`**：设计把它列为**可选**（「不必强求」），本批未启用 —— 壳的 plan-review 是一套**接管式**呈现面，为一句确认换面不划算。
- **逐字段裁剪只用省略号、不带计数**（只有**段落级**裁剪带「已裁剪至 …」标注）：给每个封顶字段加计数会让每处多约 8 码点，实测足以顶破 600 并挤掉上述标题段；字段级裁剪的诚实性由「省略号 ＋ 段落级标注」共同承担。
- **通道 1 的实机观察项**：壳可能**本来就**画出自己的 `kind:"command"` 通用行（`dsh-client-ui-chat` 也注册了一个），而本插件**无法查询「壳渲染了什么」** ⇒ 本批按设计实现通道 1 自带的顶层节点，**「是否与壳的通用行并存」留作真机观察项**。
- **未做真机验证**：本批全部读数来自两套单元套件 ＋ 隔离夹具的红相；宿主半边要等 DSH 重启窗口，§10.2.8.3 的客户端节点要重建客户端 bundle ＋ 刷新页面，**U33 的「渲染高度不超视口」依赖窗口宽度、刻意不作单测断言**。
- **`README.md` 的**两处**读数已由架构侧同批同步**（tests 徽章 → `951 + 269`、第 863 行「当前读数」→ `951` / `269`）✓。**原记「三处」是计数不实**（2026-09-22 只读审计第 7 条纠正）：被点名的第三处「第 880 行索引里的『判据 U30–U34，未落码』」**从未进过历史** —— `git log -S 未落码 --all` 只命中 `ee7c48a` 的**提交信息**一句，`git show ee7c48a^:README.md` 里 grep「未落码」**零命中** ⇒ 不存在一处「未同步的旧读数」，那句描述已删掉。

### 🔁 同批同步的面
`lib/index.js`（解析器 R1/R2/R3 与三个默认值、`teamSessionDialogQuestion`、`askTeamSessionBatch` 的两个槽位、`TEAM_SESSION_KEY_HINT` / `TEAM_SESSION_BODY_HINT`、命令 description 与 `input.hint`）；`lib/client.js`（§10.2.8.3 通道 1 的独立 definition ＋ 渲染面）；`host-half.test.mjs` / `client-half.test.mjs`（新增 U30–U34 与上述改写）；本条目；`docs/verification-log.md`。

### 🔧 2026-09-22 修正批 —— 只读发散审计 ＋ 代码评审的 **7 条**偏差收口

起因：一次**只读发散审计**（对 §10.2.8 的交付逐条对照）与一次**代码评审**各报出偏差，**去重后 7 条**。本批只修这 7 条，别处一字不动；其中**只有第 3 条是批次行为变更**，其余是渲染面（2 / 4）、断言（1）、注释（7）与文档读数（5 / 6）。

1. **审计 #3 / §10.2.8.4 (b) 的「只给 id 形状 ＋ 计数」零实现**：修法表第 0 行换槽位时，旧实现那行 `- 会话 id：team-link-<team>-<role>-<uuid8>`（`teamSessionIdPrefix()`，`git show ee7c48a^:lib/index.js`）**连同逐角色 id 清单一起被删掉**，于是「id 形状」在框里一处不剩。修：新增 `TEAM_SESSION_ID_SHAPE` 常量，把**形状 ＋ 总数**并进「会话」那一行（**不逐角色展开**照旧：仍只列前 3 个 ＋「…等 N 个」，全量清单仍只出现在完成回报里）。预算 **+35 码点**，按 §10.2.8.4 (b)「压缩只压自述段」从**可选**的 DEFECT-4 自述行里扣（删掉行内 `（DEFECT-4）` 标记与半句冗余），**必备披露一字未动**。
2. **评审 #1 / 审计 #2 的收尾：「同现 ⇒ 报错」两套件零断言**：§10.2.8.2 那句「正文与 `task=` 同现 ⇒ 报错」经**只读审计与代码评审两次独立认定可达**（反例 `team=t task=a model=m b`：`task=` 被后续已知 `key=` 截断、其后裸 token 走 R1(a) 成正文），而修法表第 0 行换槽位之后这条行为**零断言**。修：**行为一字不改**，只补两条断言 —— 反例 ⇒ 报「任务只能给一处」并点名正文起点 `b`；引号形 `task="q" 正文` ⇒ 同样被拒。
3. **审计 #4 / §10.2.8.2 默认值第 4 条：「既无正文也无 `task=`」却仍驱动会话**：`plan.task === undefined` 时实现仍然 `followup` 一具 kickoff（= 插件替人类编了一条他没写的任务）。修：该支**不投启动任务**（drive 循环只在有任务时跑），确认框与完成回报的措辞同批改准（框里「未给正文/task=，只建会话、不投启动任务」、回报里「已创建（未给正文/task=，不投启动任务）」）。
4. **评审 #2 / U33 的 600 码点被顶破**：`model=` / `provider=` 由用户给、解析器只定形状**不定长度**，而 `teamSessionModelLine` 把它们**原样内插** ⇒ 一句 300 字的 `model=` 就把 `detail` 顶到 **789 码点**（红相实测）。修：两者并入 `TEAM_SESSION_DIALOG_FIELD_CHARS`（`model: 40` / `provider: 24`），与团队名 / cwd / 会话 id 走**同一封顶**。
5. **审计 #6 / 红相逐条读数不实**：原记「U30（9 条）/ U31（4 条）/ U33（9 条）全红」，同夹具实跑的分布是 **U30 7/9 · U31 4/4 · U33 8/9**（＋ 改写的既有 U16 10 条 ＋ DEFECT-4 槽位 1 条 = **30**）；**旧实现上本来就绿的有 3 条**，已在本条目上一条与 `docs/verification-log.md` 里逐条点名。**红相总数 30 / 22 不变。**
6. **审计 #7 /「README 三处未同步」实为两处**：第三处（第 880 行「判据 U30–U34，未落码」）**从未进过历史**（`git log -S 未落码 --all` 只命中 `ee7c48a` 的提交信息；`ee7c48a^:README.md` 里 grep「未落码」零命中）⇒ 计数改为**两处**，不存在的第三处描述删除。
7. **评审 #4（低）/ 注释失真**：`TEAM_SESSION_KEYS` 的注释称「**按 hint 列出的顺序**」，而数组顺序（`n, count, team, roles, role, task, preset, model`）与 `TEAM_SESSION_KEY_HINT` 的书写顺序（`n / team / roles / preset / model / task`）并不一致 ⇒ **改注释措辞**（不改数组、不改行为）。

**怎么验证（变异验证，本批）**：

- **修复前必红**（隔离夹具 `.test-tmp-head/`：**本批的新测试** ＋ `git show HEAD:lib/{index,client}.js` ＋ 本树 `README.md` / `package.json`，在夹具目录里跑）：`node host-half.test.mjs` → **`957 (failed: 4)`**，4 条逐字点名 —— **U33 id 形状在场**（`hasShape:false`）· **U33 长 model= 封顶**（`{cp:789, nl:9}`，评审 #2 那句）· **U30 默认值第 4 条**（`{creates:2, followups:[1,1]}`，审计 #4 那句）· **U30 默认值第 4 条（措辞同步）**。`node client-half.test.mjs` → **`269 (failed: 0)`**（本批未触浏览器半边）。
- **修复后全绿**：`node host-half.test.mjs` → **`957 (failed: 0)`**；`node client-half.test.mjs` → **`269 (failed: 0)`**。新增 **6** 条宿主断言（id 形状 1 ＋ 同现 2 ＋ 默认值第 4 条 2 ＋ 长 model= 1），基线 `951`（`957 − 951 = 6`）。
- **两条「同现」断言是活锁（单点变异）**：撤掉「任务只能给一处」那处拒绝（`if (value.task !== undefined) {` → `if (false) {`）⇒ **`957 (failed: 1)`**，**恰是**那条新断言 —— 整轮 957 条里**没有第二条**断言发现这件事（= 修前它对这条行为确实零覆盖）；撤掉引号形的「闭引号之后不许再有内容」⇒ 套件在 `host-half.test.mjs:5070`（既有断言，该处读 `.error.includes` 没有 Y7 类型护栏）**当场崩**、连 `assertion total` 都不打印 ⇒ 该分支本来就被既有断言咬住，本批那条是**第二把锁**。
- **U33 重测读数（本批，逐用例，全部 9 / 12 换行）**：常规 **591 / 600 码点**、8 会话 ＋ 3000 字正文 **577**、长 cwd **593**、39 字团队名 **534**、团队名与 cwd 都长 **585**、DEFECT-4 槽位用例 **596**、无正文无 `task=` **563**、**300 字 `model=` 564**；`question` 一律单行（27 / 37 / 38 / 46 码点，上限 120）。
- **夹具输入的同批调整（判据文本一字未改）**：五具**原本既无正文也无 `task=`** 的夹具（DEFECT-1 降级 / DEFECT-2 降级 / DEFECT-4 服务缺席降级 / DEFECT-4 rename 抛错 / U17 保留）补上了 `task=` —— 它们断言的是「缺服务时**照建照驱动**」，而新默认下这一支**不再驱动**；补 `task=` 是**保住原有覆盖**，不是把断言改软（新默认自己有默认值第 4 条那两条断言）。

**如实标注（本批）**：

- **必备披露块在最极端的组合下仍可顶破 600 码点（设计缺口，可复现）**：per-field caps 那套「保证**每一个**输入都装得下」的说法**修前就已经不成立** —— 实测（20 字团队名 ＋ 8 个已登记角色 ＋ 300 字 `model=` ＋ 60 字 `preset=` ＋ 3000 字正文 ＋ 长 cwd ＋ 60 字协调者 id）**修前 633 码点**，补回 id 形状行之后 **668 码点**，而 `dialogFits` 的裁剪**只丢可选自述段**（设计明令必备披露永不裁剪）⇒ 这一组合下 `detail` 必然超预算。本批**不自行发明**「裁剪必备块」的机制（那是设计面的事），如实记为**残余**；U33 的单测覆盖的是上面枚举的那批用例（最大 596）。
- **本批未改设计档**（`docs/collab-enhancements-design-2026-09-19.md` 不在本批文件清单内）：§10.2.8.2 默认值第 4 条与 §10.2.8.4 (b) 本来就是本批实现的依据，**没有需要改写的设计语句**；唯一与设计现状有出入的是上一条残余（设计未规定「必备块超预算」的处置）。

**同批同步的面（本批）**：`lib/index.js`（`TEAM_SESSION_ID_SHAPE`、会话行的 id 形状、`teamSessionModelLine` 的两个封顶、`TEAM_SESSION_DIALOG_FIELD_CHARS` 的两个新键、`teamSessionTaskNote` 与「确认则」两支措辞、drive 循环的任务守卫、完成回报的第三种结果、`TEAM_SESSION_KEYS` 注释）；`host-half.test.mjs`（6 条新断言 ＋ 5 具夹具补 `task=`）；本条目末节；`docs/verification-log.md`（§10.2.8 一节的逐条读数与新增的本批小节）。

### 🔧 2026-09-22 收口批 —— 交付代码评审第 2 轮 PASS 后的 **3 条**小项（🟡#1 · 🔵#3 · 🔵#2）

起因：交付代码评审第 2 轮（窄范围：修复轮新增约 63 行）**PASS**，余下三条小项都不涉及结构。本批**只收这 3 条**，别处一字不动：改动面 = 对话框的**一处文本分支** ＋ **一个 per-field 上限键** ＋ **一处注释**。设计依据同批补进 `docs/collab-enhancements-design-2026-09-19.md` —— **§10.2.8.2 默认值第 4 条**下的「同支纪律」（🟡#1）· **§10.2.8.4 (b)** 的 per-field 上限清单（`preset 24`，🔵#3）· **§10.2.8.6** 的「无任务分支保留为防御」（🔵#2）。

1. **🟡#1 / 「成本口径（保守）」那行与同一张框的承诺自相矛盾**：`teamSessionDialogText` 无条件写「成本口径（保守）：N 个会话 × 至少一个完整回合（**followup 驱动一次**，按各自模型计费）。」，而同一张框的「确认则」上一批已按 `plan.task === undefined` 分支化（无任务支写「不投启动任务」）⇒ 一面说**不投启动任务**、一面说 **followup 驱动一次**，后者会被读成「已经派活了」。修：成本行**按同一支**改写 —— 无任务支为「成本口径（保守）：本次 0 次驱动（未给正文/task=，不投启动任务；后续派活时按各自模型计费）。」，有任务支**一字不改**。判据新增在 U30 的「措辞同步」组里（`host-half.test.mjs`），并且**两侧都咬**（有任务支必须保留原意）——见下「变异 A2」。
2. **🔵#3 / `preset=` 复用角色的 8 码点上限**：`TEAM_SESSION_DIALOG_FIELD_CHARS` 没有 `preset` 键，对话框那一行读 `cap.role` ⇒ 常见 preset id 被封成 `claude-c…` 这类认不出的形状，而「模型/预设」行是 §10.2.4 的**必备披露**。修：`preset: 24` 入表（与 `provider` 同级），那一行改读 `cap.preset`。
3. **🔵#2 / kickoff 的无任务分支是防御性死分支**：`teamSessionKickoffText` 里 `perRole === undefined` 那一支对 `/team_session` **不可达** —— 唯一的驱动路径在 drive 循环处就按 `plan.task === undefined ? [] : created` 把无任务那一支整个挡掉（as-of 本批 `lib/index.js:5122`）；该函数另有导出面（`__testing.teamSessionKickoffText`，as-of `:8915`，仅供测试读数），不经那道守卫。**行为一字不改**，只在函数体首行加注释写明「不可达 ＋ 守卫在哪 ＋ 为什么保留」：将来若再加一条驱动路径（或有人挪走/放宽那道守卫），少了这一行就会让 kickoff **静默丢掉「任务」行**。

**怎么验证（变异验证，本批）**：

- **修复前必红**（隔离夹具 `.test-tmp-r3pre/`：本批的新测试 ＋ `git show HEAD:lib/{index,client}.js` ＋ 本树的 `README.md` / `package.json`，在夹具目录里跑）：`node host-half.test.mjs` → **`959 (failed: 2)`**，恰是本批的**两条**新断言，逐字点名 —— **U33 长 preset= 封顶**（`{"cp":595,"nl":9,"presetLine":"… · preset=ppppppp…"}`：60 字取值被封成 **7 字 ＋ 省略号**，即复用了角色的 8 码点）· **U30 默认值第 4 条（措辞同步 · 成本行）**（`{"noTaskCostLine":"成本口径（保守）：2 个会话 × 至少一个完整回合（followup 驱动一次，按各自模型计费）。",…,"cp":563,"nl":9}`）。既有 957 条在夹具里全绿 ⇒ 两条新断言是**加锁**，不是改写既有判据。
- **修复后全绿**：`node host-half.test.mjs` → **`959 (failed: 0)`**；`node client-half.test.mjs` → **`269 (failed: 0)`**（本批未触浏览器半边）。新增 **2** 条宿主断言（`959 − 957 = 2`，基线取上一批自报的 957）。
- **两条新断言都是活锁（单点变异，三具）**：**A（🟡#1 回退：成本行改回无条件那一句）** ⇒ **`959 (failed: 1)`**，**恰是**新的成本行断言（整轮 959 条里没有第二条发现这件事）· **A2（假修法：把「followup 驱动一次」半句在**两支上**都抹掉）** ⇒ **`959 (failed: 2)`**，新断言仍在列（它**两侧都咬**：有任务支必须保留原意），另带一条既有断言（U16 确认框的成本口径）⇒ 这条锁住的是「**按支写准**」，不是「把话删掉」· **B（🔵#3 回退：`cap.preset` → `cap.role`）** ⇒ **`959 (failed: 1)`**，**恰是**新的 preset 断言。三具变异逐字回退后均复跑 `959 (failed: 0)`。
- **`preset` 上限直接量到**：60 字取值 ⇒ 框里 `preset=` 后是 **23 字 ＋ 省略号**（上限 24），不再是上一批的 **7 字 ＋ 省略号**（上限 8）。
- **U33 重测读数（本批，逐用例，`detail` 码点 / 换行）**：常规 **591/600**（9/12）· 8 会话 ＋ 3000 字正文 **577** · 39 字团队名 **535** · 110 字 cwd **594** · 两者都长 **585** · DEFECT-4 槽位用例 **597** · 既无正文也无 `task=` **564** · 300 字 `model=` **564** · **60 字 `preset=` 543（本轮新增用例；自述段被裁剪、标注在场）**。**无任务支那几条各 +1 码点**（成本行 49 → 50：改写后的措辞比原句更短，抵消了「0 次驱动」与「不投启动任务」两处新信息）。
- **同支纪律的代价（实测，如实）**：改成同支之后，无任务支的成本行**只许长到 53 码点** —— DEFECT-4 槽位用例（`n=2 team=title-dialog roles=worker-a,worker-b`，正是无任务支）的交付预算是 **597/600**，余量 3 码点。第一稿照父侧示例写满「后续由主会话派活时按各自模型计费」（59 码点）会让**正文（必备块 ＋ 标题段）**达到 **606 > 600** ⇒ **裁剪丢掉可选的自述段**、让既有的「DEFECT-4 ② 确认框说明设了什么标题」断言当场变红（本批实测复现）。故取**同义更短**的措辞（50 码点，净 +1）：既守住「不得自相矛盾」这条判据，**又不用**改那条既有断言、也不用给它补 `task=`。
- **红线回归（本批）**：`lib/` 内 `ctx.session`（**单数**）/ `appendEvent` / `session.log` / `.append(` 仍各 **0** 命中；模块级 `inject` 仍恰 **4** 项；既有 schema key、投递门与 `source` 三成员、`/team_rotate` 文法面、浏览器半边（`lib/client.js` 一字未改）**均未触碰**；**未新增任何日志事件类型**。

**同批同步的面（本批）**：`lib/index.js`（成本行的两支、`TEAM_SESSION_DIALOG_FIELD_CHARS` 的 `preset: 24` 与那一行的 `cap.preset`、`teamSessionKickoffText` 的防御性注释）；`host-half.test.mjs`（2 条新断言）；`README.md`（tests 徽章与「当前读数」）；`docs/collab-enhancements-design-2026-09-19.md`（§10.2.8.6 那条的两处行号按本批 as-of 更新）；`docs/verification-log.md`（本批小节）；本条目末节。

**如实标注（本批）**：

- **`README.md` 的两个读数此前落后一批**：0.3.10 修正批把套件自报值推到 **957**（`951 + 6`），但那一批的「同批同步的面」里**没有 README**（只列了 `lib/index.js` / `host-half.test.mjs` / 本条目末节 / `docs/verification-log.md`）⇒ README 停在**上上批**的 `951`。本批按实测把徽章与「当前读数」一并改成 **`959`**（`957 + 2`，本批新增 2 条），并把这次**跨越**写在这里 —— 不要读成「本批新增 8 条」。
- **无任务支成本行的具体措辞属实现定夺**：设计只给判据（不得自相矛盾、须与「确认则」同支），未逐字规定文案；本批取「本次 0 次驱动（未给正文/task=，不投启动任务；后续派活时按各自模型计费）。」，长度受上一条实测约束（≤ 53 码点）。
- **本批未做真机验证**（与前两批同口径）：全部读数来自两套单元套件与隔离夹具；本批两处改动都只影响**确认框文本**，宿主半边仍要等 DSH 重启窗口才在真机生效。
- **本批未动**「裁剪标注不参与预算」那条已知残余（§10.2.8.4 残余行的「第二机制」）：`teamSessionDialogText` 的裁剪/标注逻辑与 `TEAM_SESSION_DIALOG_MAX_*` 两个常量**一字未改**，该残余仍待裁。

### 🔧 2026-09-22 会诊 #68 批 —— 裁剪标注不参与预算（U33 口径定档：**交付 `detail`**）

起因：上一批的**如实标注**里记下的「第二机制」残余 —— **裁剪标注行不参与 600/12 预算**（父侧独立实测，两轮评审均未发现）。会诊 **#68**（四份意见一致）已裁定修法，父侧把裁定写进设计档 **§10.2.8.4 (b) 的 (b) 行 / 残余行**与 **§10.4 的 U33 行**（三处，**本批不改设计档**，以那三处为唯一规格）。改动面 = 对话框终态的**构造**（一处）＋ 标注的**文案与档位**（一处）＋ 一处注释；`TEAM_SESSION_DIALOG_MAX_CODE_POINTS` / `MAX_NEWLINES` 的**数值一字未动**，**必备披露块一字未裁**。

1. **修了什么（结构：终态真过预算）**：被弃路径的终态此前是 `let text = candidate(kept, dropped)`（as-of `0f77d0a`：`lib/index.js:4754`）—— body 装进 600/12 **之后**才贴上标注，**不再过 `dialogFits`** ⇒ 交付 = 必备 body ＋ 标注（旧文案 **41 码点 ＋ 1 换行**）⇒ **凡 body > 558 即溢出**（与其内容无关，故不靠「某个输入的读数」立论）。修：**拟合循环与终态走同一个构造** —— `candidate(list, stillDropped)` 现在**自己**带预算判定（在档位表上 `find` 第一个装得进 `dialogFits` 的档，取不到就用最窄那一档），终态就是调它，于是「装得下」这句话说的是**交付串**（含标注），不是标价前的正文。
2. **修了什么（标注档位化）**：两档写成**具名常量** `TEAM_SESSION_DIALOG_CROP_NOTE_FULL` / `TEAM_SESSION_DIALOG_CROP_NOTE_MINIMAL`（由 `__testing.TEAM_SESSION_DIALOG_CROP_NOTE_TIERS` 导出，供单测直接量长度），模板按**实际 `dropped` 位数**算 —— 一位数时**全档 21 码点**、**最小档 11 码点**（各另加 1 换行），两位数各 +1，代码里没有写死的 21／11。选择策略 = **取能装进 `dialogFits` 的最宽档**；**两档都装不下时仍挂最小档交付** —— 那是**如实标注的受限超额**，且此时超额**只**由不可裁的必备 body 引起（机制一残余）。**任何档位都不再写「已裁剪至 600 码点 / 12 行上限」**：交付串可能正是超 600 的那一条，那句话**自证矛盾**。
3. **改掉一句假注释**：`teamSessionDialogText` 的 docstring 曾称那组 per-field caps 「is what makes the required set fit the budget for **EVERY** input」—— 实测反例（body 693）已证伪 ⇒ 改成**如实口径**（caps 让**常规与已验证的极端**装得下，**不是**对任意输入成立的不变式），并指回设计档 **§10.2.8.4 的残余行（第一机制）**。同一句话说法的**另两处**同批改准（`TEAM_SESSION_DIALOG_FIELD_CHARS` 的注释、`host-half.test.mjs` 里引用该理由的注释）—— **三处由同一条事实驱动**。

**为什么**（会诊 #68 的裁定理由，父侧核实）：600 的用途是「**人读到的那一行有界**」，而标注**自己就写着 600** ⇒ **标注必须计价**；不修的话，「合法输入 ⇒ 交付超 600」与「框里写着已裁剪至 600 码点」并存 —— **输出物自述与事实相反**。不变量收敛为一条：**交付 ≤ max(600, 必备 body ＋ 12)**（12 = 最小档 11 ＋ 1 换行）——「**标注永不是首个破约者**」。

**怎么验证（变异验证）**：

- **修复前必红**（隔离夹具 `.test-tmp-u33pre/`：**本批的新测试** ＋ `git show HEAD:lib/{index,client}.js` ＋ 本树 `README.md` / `package.json`；`HEAD` = `8991fc8`，夹具里的 `lib/index.js` 与 `git show HEAD:lib/index.js` **逐字节相同**已核对）：`node host-half.test.mjs` → **`964 (failed: 6)`**，六条逐字点名 —— 改写后的既有 **U33 长团队名裁剪标注**（`{"cp":535,"nl":9,"note":false,"contradictory":true}`：新文案不在、旧文案还在）· 改写后的既有 **U33 两者都长**（`{"cp":585,"nl":9}`）· 新的 **U33 标注档位**（`{"tiers":0,…}`：旧树上这两个常量**根本没导出**）· 新的 **U33 (i)**（`{"bodyCp":582,"cp":624,"nl":9}`）· 新的 **U33 (ii)**（`{"bodyCp":592,"cp":634,"nl":9}`）· 新的 **U33 (iii)**（`{"bodyCp":693,"cp":735,"nl":9}`）。`node client-half.test.mjs` → **`269 (failed: 0)`**（本批未触浏览器半边）。
  - **其中一条新断言在旧树上本来就绿（如实记，沿用修正批的同一口径）**：**U33 (iii) 机制一的成因可读** —— 它量的是「3 个角色已登记 ⇒ `（已登记，跳过）` 后缀 ＋ 必备披露在场」这件**既有行为**，不是本批引入的语义。
- **修复后全绿**：`node host-half.test.mjs` → **`964 (failed: 0)`**；`node client-half.test.mjs` → **`269 (failed: 0)`**。新增 **5** 条宿主断言（`964 − 959 = 5`，基线取上一批自报的 959）；**改写 2 条既有断言**（强度只增：标注在场由「逐字等于**全档常量**的产物」咬住 ＋ 两个预算仍然都断言 ＋ 新增「不含那句自证矛盾的话」）。
- **两条单点变异（`(i)` 必须红，逐条读数）**：
  - **(a) 去掉终态的 `dialogFits` 检查**（`notes.find((note) => dialogFits(note)) ?? notes[notes.length - 1]` → `notes[0]`）⇒ **`964 (failed: 3)`**，**恰是 (i)** `{"bodyCp":582,"cp":604,"minimalTier":false}` **/ (ii)** `{"bodyCp":592,"cp":614}` **/ (iii)** `{"bodyCp":693,"cp":715}` —— **(i) 当场变红** ✓，整轮没有第四条断言发现这件事。
  - **(b) 只留全档（删掉最小档回退）**（档位表只剩 `…CROP_NOTE_FULL`）⇒ **`964 (failed: 4)`**，除上述三条外**多红一条**：新的 **U33 标注档位**（`{"tiers":1,…}`）。**两具变异逐字回退后均复跑 `964 (failed: 0)`。**
- **U33 逐用例重测（本批，读数取自交付串 `detail`，码点 / 600，换行 / 12）**：

  | 用例 | 本批 `detail` | 上一批（收口批） | 变动 |
  | --- | --- | --- | --- |
  | 常规 `n=2 team=night-shift roles=worker-a,worker-b task=做接口` | **591**，9 | 591 | 0（自述段全装下 ⇒ 本来就无标注）|
  | 8 会话 ＋ 3000 字正文 | **557**，9 | 577 | −20（全档 22 取代旧标注 42）|
  | 39 字团队名 | **515**，9 | 535 | −20 |
  | 110 字 cwd | **594**，9 | 594 | 0（无标注）|
  | 团队名与 cwd 都长 ＋ 8 会话 ＋ 500 字正文 | **565**，9 | 585 | −20 |
  | DEFECT-4 槽位 `n=2 team=title-dialog roles=worker-a,worker-b` | **597**，9 | 597 | 0（无标注）|
  | 既无正文也无 `task=`（`team=t n=2`） | **564**，9 | 564 | 0（无标注）|
  | 300 字 `model=` | **544**，9 | 564 | −20 |
  | 60 字 `preset=` | **523**，9 | 543 | −20 |
  | **(i) 装得下的最坏夹具（必备 body 582）** | **594**，9（**最小档**：全档 604 装不下）| — | 新增（改动前 **624** ✗）|
  | **(ii) 带内夹具（必备 body 592）** | **604**，9（＝ 必备 body ＋ 12）| — | 新增（改动前 **634** ✗）|
  | **(iii) 机制一夹具（必备 body 693）** | **705**，9（＝ 必备 body ＋ 12）| — | 新增（改动前 **735** ✗）|

  **超 600 的两行与为什么**：**(ii) 604** —— 必备 body 592 已落在 (588, 600] 这条带里，600 − 588 = 12 的余量装不下 21 码点的全档 ⇒ 交付**最多**超 **body ＋ 12 − 600 = 4**，且**必带标注**（这正是会诊 #68 定下的那条不变式）✓；**(iii) 705** —— 机制一残余：**必备块本身就 693**，而它**永不裁剪** ⇒ 超额**只**由必备 body 引起（设计档 §10.2.8.4 残余行 · 第一机制），本批**不**发明「裁剪必备块」的机制。其余十具**全部 ≤ 600**。**`question` 一律单行**（本批 27–46 码点，上限 120）。
- **红线回归（本批）**：`lib/` 内 `ctx.session`（**单数**）/`appendEvent`/`session.log`/`.append(` 仍各 **0** 命中；`sessionQuery` **8**、`commands.register` **3**（与上一批同读数）；模块级 `inject` 仍恰 **4** 项；**未新增任何日志事件类型**；既有 schema key、投递门与 `source` 三成员、`/team_rotate` 文法面、浏览器半边（`lib/client.js` **一字未改**）**均未触碰**。

**如实标注（本批）**：

- **历史读数不改**：本条目上两节（修正批 / 收口批）的 U33 读数表是**同一棵树不同时刻**的读数，按本仓「只追加、不重写历史」的纪律**一字未动**；凡**标注在场**的用例本轮各 **−20** 码点（41 → 21），**无标注**的用例**一字未变** —— 全量新表见本节「**U33 逐用例重测（本批，读数取自交付串 `detail`…）**」那张表。
- **(b) 行的「取最宽档」与残余行的「两档都装不下仍挂最小档」并不冲突**：前者的「装得下」指 `dialogFits`，后者是**两个承诺（有界 / 必带标注）不能同时成立**时选了「标注在场」（会诊 #68 的裁定），代价是**如实标注的超额**。
- **本批未做真机验证**（与前三批同口径）：全部读数来自两套单元套件与上述隔离夹具；本批只改**确认框文本的构造**，宿主半边仍要等 DSH 重启窗口才在真机生效；「渲染高度不超视口」照旧是设计显式降级的**真机项**。
- **本批未改设计档**：`docs/collab-enhancements-design-2026-09-19.md` 不在本批文件清单内 —— §10.2.8.4 (b) / 残余行与 §10.4 的 U33 行是**父侧刚写的规格**，本批逐字实现它。

**同批同步的面（本批）**：`lib/index.js`（终态构造 `candidate` 的预算判定、标注两档的具名常量与 `teamSessionDialogCropNote(dropped, tier)`、`__testing` 的档位导出、三处注释口径）；`host-half.test.mjs`（5 条新断言 ＋ 2 条既有断言的文案与强度 ＋ 一处理由注释）；`README.md`（tests 徽章与「当前读数」）；`docs/verification-log.md`（本批小节：夹具、病灶 `lib/index.js:4754`（as-of `0f77d0a`）、变异表、U33 逐用例重测表、红线复测）；本条目末节。

---

### 🔧 2026-09-22 裁定 A 批 —— 确认框收紧（U33c：参照夹具 **≤6 行 / ≤350 码点**）

> **判据后于同日放宽到 ≤380**（偏差修复轮把有任务支被漏删的「按各自模型计费」放回，+8 码点）：本节读数按「只追加、不重写历史」原样保留，凡与修复轮读数不一致处以下一节的「偏差修复轮」为准（参照夹具复算 **353 / 6 行**、28 码点 id **369 / 6 行**、机制一 **615**）。

**设计（唯一事实源）**：`docs/collab-enhancements-design-2026-09-19.md` **§10.2.8.4 修法表 (c) 行的「已裁（2026-09-22 用户裁定 = A）」**（父侧刚写入，本批**不碰设计档**）· 判据 **§10.4 U33** · 同表 §10.2.8.7 裁定 2（角色指引行）。逐条红相/绿相读数见 `docs/verification-log.md` 的「§10.2.8 裁定 A 批」一节。

**① 改了什么**（三处，全在本插件；宿主半边 ⇒ 需 DSH 重启才在真机生效）：**确认框正文从 10 行收到 6 行、参照夹具 569 → 345 码点**。

- **「- 会话标题：…」自述段整段移出框体**（它本来就是框里唯一的 `required: false` 段）。**读数没有丢**：它落在**完成回报**的 `- 标题（DEFECT-4）：…` 那一行（该行**改前就在**、本批一字未动）。
- **解释性括号与 § 号压掉**（用户点名要压的那几处，逐条见下表）：「（绕过发送方审批与接收方 ask 两道门，§10.2.3 预置配对）」「（保守）」「未给 model=/provider ⇒ 两半都解析并带上宿主缺省模型选择」「不设标题时它们会全都显示为工作区名；想改随时在壳里重命名」，另加两处同类的字数：`工作目录（cwd）：` → `cwd：`（同义 gloss）、**没被截断时**不再把「完整正文会原样作为启动任务投递」重复一遍（它已经显示在眼前）。
- **九件必备事实落进六行**（不是删事实，是并同族）：① 数量 ＋ 角色与 id 形状 · ② 模型/预设 ＋ cwd · ③ 启动任务 · ④ 角色指引（**单独一行**，§10.2.8.7 裁定 2 要求「各加一行」）· ⑤ 成本口径 ＋ 信任授予 · ⑥ 确认则。**§10.2.4 必备五件（数量 / 模型 / cwd / 成本 / 信任授予）一件不少**，`共 N 字` 标注、确认则两支、id 形状与计数全部在场（逐件点名断言，见下）。

| 位置 | 改前 | 改后 |
| --- | --- | --- |
| 标题自述段 | `- 会话标题：worker-1（标题：…）——不设标题时它们会全都显示为工作区名；想改随时在壳里重命名。` | **整段移出**（完成回报承载同一读数）|
| 数量 / id 形状 | 两行（`将创建 N 个…。` ＋ `- 会话 id：<形状>（共 N 个）：…`） | **一行** |
| 模型 / cwd | 两行，模型行含教学式说明 | **一行**：`- 模型/预设：宿主缺省；cwd：…` |
| 成本 / 信任授予 | 两行，含「（保守）」与「§10.2.3 预置配对」 | **一行**，虚词压掉、事实保留 |
| 角色指引 / 确认则 | 两行 | **两行**（角色指引单独一行不变）|

**② 为什么**：真机上确认框**动作可达**（缺陷③的原始症状未复现 ✓），但用户实测判「**有点大，文字也太多了，导致选项被积压**」✗ —— 同一输入的 `detail` 实测 **569 码点 / 10 行**（用户截图那次协调者 id 取满 28 码点 ⇒ **585**），用满 600 预算。用户裁定**方案 A**（收方向，不是只移标题段、也不是把披露全搬进回报）。

**③ 怎么验证**（两套件实测 `966 (failed: 0)` / `269 (failed: 0)`；基线 `964` / `269`）：

- **红相**（隔离夹具 `.test-tmp-u33a/`：本批新测试 ＋ `git show HEAD:lib/index.js`）：`966 (failed: 15)`，其中 **U33c 两条**（参照夹具 **569 码点 / 10 行** > 350/6 ⇒ 红；删除面 10 条**逐条全部还在** ⇒ 红）· **U33 必备披露逐件点名**（缺 cwd / 成本 / 信任授予 / 确认则（两支）——旧文案的字面与新判据不同）· **(i)/(ii)/(iii)** 三具夹具（旧实现 594 / 604 / 705）· 以及**改写措辞**影响的既有断言 **9** 条（2 ＋ 1 ＋ 1 ＋ 1 ＋ 1 ＋ 1 ＋ 1 ＋ 1；**计数勘误**：此处原写「8 条」，而右栏与 `.test-red-a.log` 的 15 行 FAIL 都指向 **9** 条，2026-09-22 偏差修复轮同批改准，构成见 `docs/verification-log.md`）。**客户端 269 (failed: 0)**（`lib/client.js` 本批一字未改，红相不该动它）。
- **变异验证两条**（新断言是不是活锁）：**(a) 把删掉的解释性文字加回一处**（信任行补回「（绕过发送方审批与接收方 ask 两道门，§10.2.3 预置配对）」）⇒ `966 (failed: 3)`：**U33c 参照夹具当场变红（379 > 350）** ＋ 删除面点名两条 ＋ U16 信任授予那条；**(b) 删掉一件必备披露**（整句「信任授予：…」）⇒ `966 (failed: 3)`：**U33 必备披露逐件点名当场变红，并逐件点名缺的是「信任授予」** ＋ 机制一夹具守卫（body 551 不再 > 600）＋ U16 信任授予那条。两具逐字回退后复跑 `966 (failed: 0)` ✓。
- **红线回归**：`lib/` 内 `ctx.session`（**单数**）/ `appendEvent` / `session.log` / `.append(` / `ctx.commands` 仍各 **0** 命中；`sessionQuery` / `commands.register` 仍 **8** / **3**；模块级 `inject` **仍恰 4 项**；`TEAM_SESSION_DIALOG_MAX_CODE_POINTS` / `MAX_NEWLINES` **数值一字未动**（600 / 12）；**必备块一字未裁**（裁剪机制只动可选段）；既有 schema key / 投递门 / `source` 三成员 / `/team_rotate` 文法面未触碰。

**④ 必须知道的一件事（如实标注，不是本批能决定的）**：移走「会话标题」段之后，框里**再没有** `required: false` 的段 ⇒ `dropped` 恒为 0，`dialogFits` 与两档裁剪标注**当前没有取用路径**（代码按用户裁定的「不许动：…两档标注与终态预算机制」**一字未改**地留着；两档文案的长度与「不含自证矛盾句」仍由一条**纯函数**断言咬住；一旦某段自述回到框里，取用路径立刻恢复）。同批把 U33 (i)/(ii)/(iii) 三具夹具改写成**现在真正成立的形状**：交付 `detail` **逐字等于必备 body**（不裁剪、不追加标注），机制一那一档（必备 body 607 > 600）**仍然必然超线**、超额只由不可裁的必备块引起。**此项已作为「需要父侧决定」的第一条写进交付报告。**

**⑤ 同批同步的面（本批）**：`lib/index.js`（`teamSessionDialogText` 的六行构造 ＋ `teamSessionModelLine` / `teamSessionTaskNote` 的措辞 ＋ 五处注释口径）；`host-half.test.mjs`（新增 U33c 两条断言 ＋ 必备披露**逐件点名**断言 ＋ 改写 13 条既有断言的文案/形状）；`README.md`（tests 徽章与「当前读数」→ `966`）；`docs/verification-log.md`（本节）；本条目。

### 🔧 2026-09-22 偏差修复轮 —— 独立发散审计 `DIVERGENCE(5)` 收口

**设计（唯一事实源）**：`docs/collab-enhancements-design-2026-09-19.md` §10.2.8.4 修法表 (c) 行的「落码读数 …… **判据定档**」与「**380 的来由**（审计 DIVERGENCE #4 后定档）」＋ §10.4 U33 行末的「裁定 A 落码后的口径更正」（父侧同批写入，本批**不碰设计档**）。**任务书就是偏差清单**：独立发散审计判 `DIVERGENCE(5)` —— 实现面的九个必备事实与裁剪机制**无偏差**，5 条全在读数/计数/规格口径/未申报删字/恒真断言上。逐条红相/绿相读数见 `docs/verification-log.md` 的「§10.2.8 偏差修复轮」一节。

**① 改了什么（五条偏差逐条 ＋ 一项附带）**：

1. **放回一条被漏删的必备事实**（偏差 4，**先修**）：有任务支成本行原为「成本：N 个会话 × 至少一个完整回合（followup 驱动一次，**按各自模型计费**）」，裁定 A 落码时把「按各自模型计费」**一并删掉**了 —— 它**不在裁定清单里**，且属**成本口径**（每队各按自己的模型计费；无任务支一直保留着它）。⇒ **放回**（`+8` 码点，行数仍 6）。
2. **判据口径写齐**（偏差 3）：设计档定档 **≤ 6 行（硬）且 ≤ 380 码点**，而实现注释与 U33c 断言仍写 ≤350 ⇒ 两处同批写到 **380**，并**如实留下 350 → 380 的放宽与理由**（换回一条必备事实；**不是**「本来就 380」）。
3. **删掉一条恒真断言、换成正面判定**（偏差 5，**选 (a)**）：`u33RequiredBodyOf(detail) === detail` 这类「右边从 `detail` 自己算出来」的写法在裁定 A 之后**结构上恒真**（注释却称它防「假绿」）⇒ 包装删除，改对**交付串本身**正面断言「框内**没有**段落级标注」（标注形状行数 = 0），两条同类反锁一并核过。
4. **文档读数复算**（偏差 1）＋ **计数勘误**（偏差 2）：「全夹具逐用例读数」表有 **7 行**用了 **14 码点 cwd**（真夹具的 cwd 字段封顶 **24**）⇒ 真夹具复算（该 7 行**两列各 +10**）；「既有断言 **8** 条」应为 **9**（`docs/verification-log.md` 病灶表与本节 ③ 同批改准，红相 15 的构成 = U33c 2 ＋ 逐件点名 1 ＋ (i)(ii)(iii) 3 ＋ 既有 **9**）。
5. **附带**：`lib/index.js` 里说 `dialogFits`「常规路径上不再被调用」的注释改为如实口径（**任何路径都不被调用**）。

**② 为什么**：这 5 条里有 3 条是**我们自己的账没记准**（读数、计数、规格口径），1 条是**没申报就删了用户要的事实**，1 条是**把恒真式说成守卫**。用户的裁定只授权「压掉解释性文字与整段自述」，**没有**授权删成本口径；判据从 350 放宽到 380 是**有代价的放宽**（换一条事实的回归），必须写明来由而不是悄悄改数。

**③ 怎么验证**（两套件实测 `966 (failed: 0)` / `269 (failed: 0)`，与修复前**同计数**）：

- **红相**（隔离夹具 `.test-tmp-fix/`：本批新测试 ＋ `git show HEAD:lib/index.js`）：**`966 (failed: 3)`**，三条全咬放回的那条事实 —— U33c 参照夹具（成本口径那半句）· U33 必备披露逐件点名（`（缺：["成本"]）`）· U30 成本行同支纪律。
- **变异验证两条**：**(A) 把段落级标注人为塞回** ⇒ `966 (failed: 6)`，6 条**全部**点出那行标注（U33 (i)(ii)(iii) · 长团队名 · 两者都长 · U33c 7 行）⇒ **新的正面断言可假、不是恒真** ✓；**(B) 把一处解释性文字加回**（+34 码点）⇒ `966 (failed: 3)`：**U33c `387 > 380`** ✓ ＋ 删除面点名 ＋ U16 信任授予。两具逐字回退后复跑 `966 (failed: 0)` ✓。
- **复算读数（真夹具，24 码点 cwd）**：参照夹具 **353 / 6 行**（12 码点 id）与 **369 / 6 行**（**28 码点 id = 真机那一次**；原 585 / 10 行）；机制一残余必备 body **607 → 615**（仍 > 600，永不裁剪）。
- **红线回归**：`ctx.session`（**单数**）/ `appendEvent` / `session.log` / `.append(` 仍各 **0**；`sessionQuery` / `commands.register` 仍 **8** / **3**；模块级 `inject` **仍恰 4 项**；`TEAM_SESSION_DIALOG_MAX_CODE_POINTS` / `MAX_NEWLINES` **数值一字未动**（600 / 12）；裁剪机制与必备块**未触碰**；既有 schema key / 投递门 / `source` 三成员 / `/team_rotate` 文法面未触碰；`lib/client.js` 与 `README.md` **一字未改**（断言总数未变 ⇒ 徽章与「当前读数」本就正确）。
- **同批同步的面（本批）**：`lib/index.js`（成本行放回半句 ＋ 四处注释口径：判据定档/380 的来由/块内判据/`dialogFits` 调用面）；`host-half.test.mjs`（U33c 判据 350→380 ＋ 放宽理由注释 ＋ 恒真包装换成正面判定 ＋ 成本口径三处判据改写，**断言条数不变**）；`docs/verification-log.md`（病灶表计数 8→9 ＋ 复算表 ＋ 新增「偏差修复轮」一节）；本条目。
- **如实标注**：**未新增/删除任何断言**（`966` / `269` 不变 ⇒ `README.md` 的两处读数不需要动，也没有动）；**未做真机验证**（宿主半边，需 DSH 重启）；**未改设计档**。

### 🆕 §10.2.8.9 / §10.2.8.10 三条新语义落码（2026-09-22 用户裁定）

> 设计（唯一事实源）：`docs/collab-enhancements-design-2026-09-19.md` **§10.2.8.9**（① 默认团队名带日期 · ② 现任失联则释放并认领团队名）与 **§10.2.8.10**（① 调用方回执 = 通道 4 · ② 无任务也投最小唤醒）。验收落在 `host-half.test.mjs` 的 **U35 / U36 / U37** 与「**U32 通道 4**」两组 ＋ U30 默认值第 4 条的**反转**三条。

**① 改了什么**

- **默认团队名带日期（§10.2.8.9 ①）**：`/team_session` 省略 `team=` ⇒ `<basename(cwd)>-YYYYMMDD`（本地时区，**一条命令只取一次**；目录名不合 `[a-z0-9-]+` 回退 `default-YYYYMMDD`）。**孤立后果如实**：**无日期的既有团队仍完全有效**，但只能用**显式 `team=`**（或设置 UI）触达 —— **默认名永不再解析到它们，且不做任何迁移**。`team` 字段封顶 **20 → 32**（带日期的默认名 29 码点必须完整显示）。
- **现任失联 ⇒ 释放并认领（§10.2.8.9 ②）**：**只有确证**「已归档」（`workspaceRegistry.archivedSessionIds` 含它）**或**「已不存在」（`sessionQuery.filterSessions([{kind:"id",values:[…]}])` 返回空）才放行；动作是**同一道命令里**把那一段任期收口（`until=now` ＋ 备注 `released: coordinator archived|gone`）→ `current := null` → 调用会话认领。**保留既有角色与版本史**，`policy.writer` **仍是 `coordinator`**（不放宽成 `any`）。**fail-safe 两情形互斥**：**(甲) 服务未提供** ⇒ 跳过归档信号、只按 `gone` 判；**(乙) 服务在但读不到** ⇒ `unknown` ⇒ **拒绝**。**落笔前复检一次**（现任复活 / 归档态变了 / 现任换人 ⇒ **中止且零写入，连 pairs 也不写**）。**只看现任**：换届换下的旧任归档**不**触发。
- **调用方回执（§10.2.8.10 ①，通道 4）**：命令**结算后**给**调用方会话**投一条**一行 ≤120 码点**的回执（**成功 / 失败 / 取消三态都算**），信封沿用 kickoff 那条**恰三成员**的 relay。**解析前失败**（如 `n=abc`）**不硬塞团队名**。
- **无任务也投最小唤醒（§10.2.8.10 ②，默认值第 4 条反转）**：既无正文也无 `task=` 时**不再「只建会话」**，改投一具**待命通知**（含①「本次命令未给任务」②「你已被创建为团队 X 的角色 Y」③「请等待主会话派活」；**不含任务内容、不要求它做事**）⇒ 每个新会话都有回合 ⇒ **在侧边栏可见**。
- **成本口径连带改写**：框内成本行由「N 个会话 × 至少一个完整回合（**followup 驱动一次**）」改写为**回合计数口径** `N＋1 个回合（N 个新会话 ＋ 1 条调用方回执；按各自模型计费）` —— 两支**逐字同形状**（有任务 N＋1、无任务也 N＋1），所以它**不再分支**，也就不可能与同一张框的「确认则」自相矛盾（🟡#1 的同支纪律由「不写任何 branch-specific 断言」满足）。**这是一次经申报的改写**：让位的是机制短语「followup 驱动一次」（机制由「确认则：创建 → 投递启动任务」承载），「**按各自模型计费**」**逐字保留**（DIVERGENCE #4 的回归）。

**② 为什么**

- 默认名带日期：用户原话「默认团队名 需要加上工作区目录+日期，这样好点」；同日重复发命令仍指向**同一个团队**（§10.2.6 幂等一字未动），跨天才自然另起一队。
- 释放并认领：用户原话「如果团队主管会话被归档（现任主管，被换届的老主管会话不算），应该自动释放团队名」——**没有这条**，一个被归档的现任会把团队名永久卡死（`writerGate` 只认现任，而现任回不来）。
- 调用方回执 ＋ 最小唤醒：真机观察 2/3/4 —— 壳只列「至少跑过一个回合」的会话 ⇒ **在空会话里发命令**、以及**不带任务建出的 worker**，都**在侧边栏隐身**。用户原话：「让插件给调用方会话投一条回执，这样才能把会话出来」＋「改回『投一具最小启动任务』」。

**③ 怎么验证**（两套件实测 `995 (failed: 0)` / `269 (failed: 0)`，基线 `971 (failed: 0)` / `269 (failed: 0)`；**+24 条断言**）

- **变异验证三条（逐条实跑，每条只红它该红的那条）**：**(A) 把 (乙) 的 `unknown` 改成「读不到就释放」** ⇒ `995 (failed: 1)`，恰好红「U37 (乙)」；**(B) 把「只看现任」改成「看任一历史任」** ⇒ 恰好红「U37 变异基线 2」（同跑另见一条**既有计时器夹具的偶发红**，见下）；**(C) 去掉写时复检里的活性那一半** ⇒ `995 (failed: 1)`，恰好红「U37 变异基线 3（TOCTOU）」。
- **红相（新测试 ＋ 旧实现，隔离夹具实跑）**：`995 (failed: 34)` —— 34 条逐条点名（U16 成本口径 1 · U33 2 · U33c 2 · U30 默认值 2 · U35 5 · U30 反转 4 · U32 通道 4 5 · U36 8 · U37 4），其中 3 条**负相**（`policy.writer` 边界 / 无新日志事件 / 只看现任）在旧实现上**本来就绿**，如实标注不算红相。**这次实跑还抓出我自己新测试的两个崩相**（未守卫的可测面读取 · 只在假分支求值的 `probe` 声明在后面 ⇒ `TypeError` / TDZ，`assertion total` 都不打印）—— 已按本仓 Y7 纪律修成「先取值再判类型」，教训记在 `docs/verification-log.md`。
- **逐具夹具读数**：**U35** 五条（格式 / 同日幂等纯函数 / 同日幂等命令级 / 跨日另起一队 / 无日期旧团队的孤立后果——**注入「当天」**，不靠墙钟）· **U36 (a)–(h)**（归档 / gone / 存活照旧拒 / 工具路径 / 保留既有角色 / 触发释放的框体 ≤6 行 ≤390 码点）· **U37 甲/乙两形状 ＋ 三条变异基线** · **U32 通道 4 六条**（成功 / 无任务形态 / 取消 / 解析前失败 / 解析后失败 / 无新日志事件）· **U30 反转三条**。**U33c 重测**：12 码点 id **355** · 28 码点 id **371** · 释放变体 **348 / 382**（28 码点 id ＋ 带日期团队名，最坏一具），判据 **≤390**（此前测试里写的 380 比设计档更严，本批与设计档对齐）。
- **真机已复验（2026-09-22 重启窗口；这一条取代原先的「未做真机验证」）**：工具面的「释放并认领」、`/team_session` 的**释放 / 回执 / 最小唤醒 / 确认框**四项、**侧边栏三会话可见**、以及**结算时序读数**（`command/done → 回执落盘 0.288 s`，回执花掉的模型回合在其后）都在真机上验到 —— 逐条读法与截图证据见 `docs/verification-log.md` 的「真机复验」一节。**这次复验还抓出一个我漏补的判据**：设计档 🟡#7 要求补一具「无任务形态」的框体读数，我漏了，而真机那一次命令恰好落在它上面 ⇒ **修前 399 码点 > 390**（重复文案所致）⇒ 压掉重复（−18）后 **381**，夹具已补（参照 F/G/H/I）。
- **如实标注**：**§10.2.8.10 的实现期兜底没被触发**（回执走的正是先试的 handler 内 `followup`）；**设计档的两处计数/读数勘误**已同批改（该披露句字面量逐字数 **11**，原写「12 码点」；§10.2.8.9 ② 的 `364 / 380` 是**改写成本行之前**的读数，重测后为 `348 / 382`）；**一次既有计时器夹具的偶发红**（「缺口1 超时读数自报真实后果（计时器到点）」，连续两次复跑均绿，与本批无关）如实记录、不计入本批证据。
- **真机收尾（同日）**：三个一次性探针团队按 `retire` 退役 —— `rm-probe-20260922` 与 `dsh-session-link-pro-20260922` 由本会话发起（后者**先经 §10.2.8.9 ② 把那具幽灵现任释放并认领，再退役**），`rm-release-20260922` 由用户在其测试会话里发起（`retire` 顺带**清掉 2 条 pairs** ⇒ 退役那一步的**信任清理**也在真机走过）；三行现均 `vacant`。**团队行本身的删除没有工具面路径**（只有用户设置 UI），本批未碰；盘上那三个黑板目录已随退役一并删除（工作区里 `team/` 是未跟踪文件 ⇒ 删除无 git 影响）。

---

## 0.3.9 — 2026-09-21（发布前收口于 0.3.10）（加固与恢复）— ① 导出路由信任栅栏 · ② 恢复能力加宽 · ③ 侧栏「会话工具」入口 · ④ 深链聚焦修复

> 设计（唯一事实源）：`docs/hardening-and-recovery-design-2026-09-21.md` §4.1–§4.4（含 §5 红线 B1–B9 与 §6 判据 U1–U14）；逐条**红相/绿相读数**见 `docs/verification-log.md`。四条工作面按 §11 分三批实施，本条目随批次追加。
> **发布状态**：①② 属宿主半边，需 DSH 重启才在真机生效；③④ 属浏览器半边（客户端 bundle 重建 + 刷新页面）。真机复验点 1 的**拒绝面已闭合**（2026-09-21 重启后复跑，原始读数见 `docs/verification-log.md`「批次 4 · H 真机复验点 1 · 绿相（重启后）」一节）；**仍未闭合的真机面**（如实列出）：① 点 1 的「**同源带 cookie → 200 全量**」正路径**无读数**；② **点 2**（`reappoint` 用 threat-intel 真实 roster 形演练）**从未执行** —— 现有判据都是单元夹具形；③ 点 3 的**标题栏收起态**未核对。而点 3 的**宽态**与**收起态**已由 owner 在 3080 页面上真机确认（宽态：位于消耗卡**右侧**、【设置】上方；收起态：56px 轨道下只剩图标、与使用卡同一行）。与 0.3.8 同口径，**不把未验的东西说成已验**。

### 批次 1 —— §4.1：导出路由接入平台信任栅栏（两层 fail-closed）

**修了什么**：`GET /team-link/export` 此前是一个**没有任何信任检查**的路由——任何能连上本机端口的请求（DNS rebinding / 跨站页面）都能拉走整份会话事件流。本批把它接上平台自己的栅栏 `connection.requestRejection`（Host/Origin → 403、浏览器鉴权 → 401），并**两层都 fail-closed**：

1. **挂载期**：路由只在**同时**拿到 `webServer` 与 `connection`（且后者有 `requestRejection`）时才注册；新增两个 reason code `no-connection` / `no-rejection`，与既有 `not-active` / `no-register` 一样——**不注册路由 + 一行 warn**（点名缺的是哪一个）。晚挂从 `ctx.inject(["webServer"])` 改成 `ctx.inject(["webServer", "connection"])`，两个提供方任意顺序到齐才挂载。
2. **请求期**：handler 的**第一句**实时复检栅栏；取不到（服务缺席、方法缺失、或**挂载之后被拆**）⇒ `503` + 不吐数据；取到则把它的状态码**原样写回**，响应体与官方 `client-connection` 的 RPC 通道一致（401 `unauthorized` / 403 `forbidden`）。这一层覆盖挂载期判不到的那个窗口。
3. **方法白名单**：非 `GET` ⇒ `405` + `allow: GET`（对齐官方路由写法），任何会话都不读。

**为什么**：平台契约明确「路由所有者自负其责」（`dsh-host-webserver`：不做 server-wide 认证/来源策略），栅栏由 `dsh-client-connection` 提供给**每个**路由所有者；本插件此前全库 0 处调用它——这是一处**未文档化的安全缺陷**，不是被记录下来的取舍。

**怎么验证（变异验证）**：

- **修复前必红**（本批新增 14 条断言，基线 889 不动）：`node host-half.test.mjs` → `assertion total: 903 (failed: 10)`。10 条 FAIL 恰是本次要修的每一面：U1 纯函数不存在 / 跨站请求真的拿到 200 + 会话正文且 `readSession` 被调用 / 路由从未把请求交给平台栅栏 / 401 分支无人写回 / 挂载后栅栏消失仍照常吐数据 / U6 非 GET 被当 GET 服务 / U2「webServer 在、connection 缺」仍然注册了路由（无门路由真实存在）/ `no-rejection` 分支不存在 / U4 只到 webServer 就挂载。
- **修复后全绿**：`node host-half.test.mjs` → `assertion total: 903 (failed: 0)`；`node client-half.test.mjs` → `170 (failed: 0)`（未触及浏览器半边）。
- **红线 B1**：`connection` **没有**进模块级 `inject`（仍恒为四项），仍走 `ctx.get` + `ctx.inject` 的可选服务缝；断言直接读导出数组。
- 新增可测面：`__testing.exportGateRejection(connection, req)` —— `connection + req → 状态码|null` 的**纯函数**判定（U1 不必依赖 HTTP 夹具；接受 `null` = 放行、`503` = 无栅栏可问）。

**如实标注（设计未规定的一处实现选择）**：`503` 这一支的响应体写的是 `unavailable`，而 401/403 严格照官方（`unauthorized` / `forbidden`）。理由写进了代码注释：403 的含义是「栅栏说了不」，503 的含义是「**没有栅栏来裁决**」——把后者写成 `forbidden` 等于报告一个从未发生过的信任判定。

**同批同步的面**：`README.md`（§二 导出段新增「下载路由走平台信任栅栏」一段、§七 契约的服务清单、依赖服务降级表新增 `connection` 行、架构图的导出路由节点）；本条目。

### 批次 2 —— §4.2：恢复能力加宽两格（(a) `revive` 放开角色门 · (c) `reappoint` 常驻「自建继任者」）

**修了什么**：

1. **(a) 删除 `reviveIncumbent` 的角色门**——`revive` 现在受理**任意角色**（所有权门、`resume` 可用性检查、在场确认框、两次写前复检**一律不动**）。
2. **(c) `reappoint` 的候选集改为「活成员 ∪ 插件自建继任者（常驻）」**——合成候选 `自建继任者（新建会话）` **始终在列**（不是「仅 0 活候选时才出现」：事故当场的候选长度是 **1 不是 0**）。选中它时逐条复用既有链路：能力闸门（**批次 4 收窄到这条支路**：宿主无 `agents.create` ⇒ 弹框照开、候选里**不出现**「自建继任者」；**仅当活成员候选也为 0** 时才 fail-closed 报告且零弹框——活成员改任那条路不需要该服务）→ 按 §10.2.2 铸 id（`team-link-<team>-<role>-<uuid8>`）→ 交接文档由**插件从 roster 事实**生成五硬节（读不到的项——前任的进行中工作与 goal、未提交的改动——**如实标未知**，不编造）→ `prepare` 逐字跑 → 审计行 `verb=reappoint`。原有的「候选集为空 ⇒ 一条死路」分支因此**不可达并被删除**。

**为什么**：(a) `revive` 是**身份不变**的操作（不写 roster、不铸令牌、不动 pairs/trustedSenders/rememberTargets、不碰 `policy`），放开角色面不向「通用 roster 编辑器」移动一寸；而重载拆掉的是**所有**插件自建会话，不只 coordinator 的——§11.9.1 只论证过「死的 coordinator 必须可救」，从未论证「死的 worker 不许救活」。(c) 候选用尽时原本**没有出口**：全队只剩协调者一个活人时，唯一可选路径是「把一个 worker 角色硬塞给协调者」。

**怎么验证（变异验证）**：

- **修复前必红**：把**本批的新测试**跑在**批次 1 的 `lib/index.js`** 上（隔离夹具 `.test-tmp/s2-red/`：新测试 + 新 README + 旧实现）⇒ `assertion total: 920 (failed: 21)`，21 条 FAIL 全部落在本批断言上：U7 两条（插件自建的死 worker 被角色门拒）、U8（**连所有权门的文案都读不到**——角色门先拒，这正是「角色门挡在适用域之前」的实证）、U9 **九条**（对话框里没有合成候选、没有创建、没有交接文档、没有三处留痕；同组那条「新建的会话是根会话、meta 无血统字段」在修复前**本来就为真**——旧实现从不 `create`，`creates` 为空——所以它**不在**红相里，批次 4 重跑实测定档）、U10（无 `agents.create` 时既没有闸门也没有零弹框）、U11 与收敛性两条，以及**六条被本批语义变更推翻的既有断言**（U26 角色面 / Y1 对照 / Y1 文档面=实现面（双向锁）/ Y2 宣传面 / U27 候选由插件算 / U27 全队皆死）= 2+1+9+1+2+6 = **21**，与 `920 (failed: 21)` 逐字对上（原文记「U9 十条 / 五条」是**计数勘误**，加总 22 与实测差一条，批次 4 已按实跑读数改准——见 `docs/verification-log.md` 批次 2 的「计数勘误」小节）——设计 §9 A7 点名的「既有断言可能把非 coordinator 一律拒钉死」**实测成立**，本批按新语义改写这六条而不是删除（每条都留了改写说明）。**红相是干净的**：套件跑到底并打印了 `assertion total`（首版红跑因直接调用尚未存在的 `__testing.selfBuiltHandoffBody` 而崩在那一行、连总数都不打印，已按本仓 Y7 纪律加类型护栏——那正是 Y7 存在的理由）。
- **修复后全绿**：`node host-half.test.mjs` → `assertion total: 920 (failed: 0)`（新增 **17** 条，`920 − 903`）；`node client-half.test.mjs` → `170 (failed: 0)`（未触碰）。
- **红线回归**：所有权门原样（U8）、`writerGate`/三道门本体未动、`policy.writer` 未动、`claim` 一字未改、不新增令牌类型、**不新增参数**（`handoff` 没有进 recover 的参数面——交接正文由插件生成）、模块级 `inject` 仍 4 项。

**如实标注（设计没有逐字规定、由实现定夺的两处）**：

- **自建继任者的工作目录**：优先用**团队自己的 `workspace`**（该队的 roster 镜像与交接文档就在它下面），团队没记录时回落**发起会话的 cwd**；两者都取不到绝对路径 ⇒ fail-closed（零创建）。`process.cwd()` 一律不用（与 `successor:"auto"` 同一条边界）。设计 §4.2 (c) 未规定 cwd 的取法。
- **交接文档与 `prepare` 的先后**：按 **§11.9.6 的 abort-before-prepare**（文档失败 ⇒ 不铸令牌）排在 `prepare` **之前**，也就是与 `successor:"auto"` 同序；§4.2 (c) 的编号（③ prepare → ④ 文档）是**元素清单**而不是时序，本批取了与既有 auto 路径一致的时序。

**同批同步的面**：`lib/index.js` 的 `team_link_recover` description（两个动词的角色面 + 候选集 + 合成候选链路 + 代价声明）、`action`/`role` 参数文案、诊断行；`README.md` 的工具表、§11.9.4 恢复叙述（角色面 + 自建继任者段）、八条硬约束的 ③；`docs/collab-enhancements-design-2026-09-19.md` §11.9.4/§11.9.5 的两处增补。

### 批次 3 —— §4.3 侧栏「会话工具」入口 · §4.4 深链聚焦修复

**修了什么**：

1. **§4.3 入口**：侧栏底部新增「会话工具」——注册进官方 list 槽位 `sidebar.footer.action`（`id: team-link-session-tools`、`order: 0`）⇒ 入口落在**同一 flex 行内、位于消耗卡片 `order: -10` 的右侧**（`order: 0` 升序 ⇒ 排在它之后、【设置】之前）；**不是「下方」**：官方 `.footerActions` 是 `display:flex`（方向默认 row），且官方 sidebar 全档 `flex-wrap` 出现 **0** 次 ⇒ **不换行**，而插件**无法从子元素侧改变父级换行**（改父级 = 改官方文件，违反设计 §1.3 的红线 N5），2026-09-21 真机读数（owner 截图）与之一致——**原稿把「排在后面」写成上下排布，是对官方布局的错误推断，本行按设计 §4.3.1 更正**。本入口**不改任何官方文件**。宽态 = 图标 + 文字「会话工具」，收起态（56px 轨道）**只渲染图标**（文字转 `aria-label`/`title`）；点击 / Enter / Space 打开官方 **`Modal`**（居中、挂 body——锚定面板会被 56px 轨道裁切）。
2. **弹窗（§4.3.2）**：标题 + 当前计数 → 搜索框 → 会话列表（可滚动）→ 有界呈现标注 → 底部（范围切换 + 关闭）。数据源 = `ctx.sessions.list` / `ctx.workspaces.list`：丢弃 `origin === 'subagent'`、丢弃已归档、blank 行只保留当前会话（官方会话浏览器同款规则），按 `updatedAt` 倒序；当前会话 = `retainedBy.mainView > 0` 的那一行；**默认范围 = 当前工作区**，可切「全部工作区」。
3. **列表行（§4.3.3）**：状态点 **恰两态**（运行中 / 空闲，唯一数据源 `SessionSummary.running`）+ 标题（超长省略）+ 相对时间（用官方 `relativeTime` 分桶、本插件字典给词；「全部工作区」时追加工作区名）；「复制链接」「导出会话」**默认隐藏，悬停该行或焦点落在该行内才浮现**（CSS `opacity` + `:focus-within`）；点行本身 = 打开该会话。
4. **三个动作（§4.3.4）**：复制 = 剪贴板 `dsh://session/<id>`（与会话头部按钮**同一格式**）+ 短暂「已复制 ✓」+ `aria-live` 播报；导出 = **导航** `GET /team-link/export?session=…&format=md`（同源自动带 cookie、天然过 §4.1 的栅栏、零 CORS 面；参数必须编码）；打开 = `ctx.uiWorkspace.openSession(id)`（**运行时**经 `ctx.inject(["uiWorkspace"], …)` 取用）。
5. **状态与边界（§4.3.6）**：三种空态**三句不同的话**（读取中… / 没有匹配「xx」的会话 / 暂无其他会话）；超过显示上限时末尾写明「**共 N 个，仅显示前 M 个（搜索可收窄）**」；`sessions` / `workspaces` / `uiWorkspace`（以及 seed 模块的 `Modal`）缺任一项 ⇒ **入口不注册 + 一行 warn**，绝不渲染假按钮。
6. **键盘与无障碍（§4.3.7）**：Tab / Enter / Space 可用；关闭（Esc / 关闭按钮 / 成功打开）**把焦点还给入口**；动作按钮带独立无障碍名（含会话标题）；尊重 `prefers-reduced-motion`。
7. **§4.4 深链聚焦**：把 `ctx.sessions.open(id)` 换成 `ctx.uiWorkspace.openSession(id)`，并**保留**「等会话出现在 `sessions.list` 再聚焦」的重试循环；服务缺席只丢「聚焦」这一步，失败只留一行痕。
8. **`package.json` 的 `dsh.client.inject`** 增三项：`@deepseek-ai/dsh-client-ui-workspace`、`@deepseek-ai/dsh-api-session-controller`、`@deepseek-ai/dsh-api-workspace-controller`。**seed 模块 `@deepseek-ai/dsh-client-ui-primitives` 刻意不声明**（与 `react` 同级由 shell 注入）。

**为什么**：官方会话行的「…」菜单**没有扩展槽位**、且硬编码在 `node_modules` 的构建产物里（设计 §2.3）——改它等于旁路官方文件且会被升级静默还原；侧栏底部那个 list 型槽位是官方**声明允许任意插件注册**的座位。而深链聚焦是一处**静默空调用**（§2.4）：`ISessions` 公开面**没有** `open` 方法（同名的 `Session.open()` 是历史加载，另一回事），调用抛出的 TypeError 被 `try/catch` 吞掉 ⇒ 列表轮询到了、界面却停在原地。

**怎么验证（变异验证）**：

- **修复前必红（整批）**：隔离夹具 `.test-tmp/s3-red/`（本批新测试 + `git show 8d40343:lib/client.js`）⇒ `node .test-tmp/s3-red/client-half.test.mjs` → `assertion total: 193 (failed: 27)`。27 条 FAIL 全部落在本批断言上：9 条 §4.3 红相守卫（入口未注册 / 入口与弹窗工厂不存在 / 五条纯规则不存在）+ `§4.3 可测面` + 9 条被新语义推翻的既有计数断言（`fresh.length` 4→5、`names()` 多一行）+ 7 条 U14。
- **U14 单独红相（设计 §6 点名的那条）**：`.test-tmp/s34-red/`（当前实现**只**把 §4.4 那一行还原成 `ctx.sessions.open(id)`）⇒ `257 (failed: 7)`，其中判据那条即「**深链打开后当前会话 id 不变**」——以 `retainedBy.mainView` 判定，主视图仍留在启动时那个会话上（另有 4 条 U14 与 1 条病灶锁同时红）。
- **修复后全绿**：`node client-half.test.mjs` → `assertion total: 257 (failed: 0)`（新增 **87** 条 = 257 − 170：10 条红相守卫/可测面 + 77 条 §4.3/§4.4 断言；另有 **9 条既有断言**在同一批里按新语义改写——`fresh.length` 4→5 与 `names()` 多一行，见下条变异验证）；`node host-half.test.mjs` → `920 (failed: 0)`（本批未触碰宿主半边）。
- **红线**：不改官方文件；本插件**模块级** `inject` 仍恒三项（`slots` / `sessions` / `locale`）——三个新依赖只进 `dsh.client.inject`（模块图）与运行时 `ctx.inject`（服务）；本入口的动作集恰为「复制链接 / 导出会话 / 打开会话」，**不做任何会话写操作**。

**同批同步的面**：`README.md`（§二 新增「侧栏「会话工具」入口」小节与三种空态表、深链段的聚焦修复指针、§九「浏览器半边的模块声明」、§十 读数与口径、设计文档索引登记本档并修正 ③b「未实施」的既有失真、版本徽章 0.3.7→0.3.8、测试徽章 889+170→920+257、架构图加侧栏入口节点与导出边）；本条目；`docs/verification-log.md` 的批次 3 条目。

**如实标注（设计未逐字规定、由实现定夺的地方）**：

> **后续变更（批次 4 G，2026-09-21）**：**列表已改为丢弃当前会话**（见下文「批次 4」第 7 条 / 设计 §4.3.2 的可见性规则）——下面这条「列表包含当前会话」的说明**已被取代**，**不再**是当前行为；**原文保留**（本文件不改写历史条目）。当前行为的验收点：工作区里只剩当前会话时显示「暂无其他会话」且一行都不画。（同批「弹窗」条目里那句「blank 行只保留当前会话」同属被取代的旧口径。）

- **列表包含当前会话**：§4.3.2 的可见性规则里没有任何"排除当前会话"的话，且它明确要求"blank 行只保留当前会话"（以当前会话**在列表里**为前提）⇒「暂无其他会话」这句出现在**可见集为空**时（例如整个工作区都已归档）；工作区里只剩当前会话时，列表如实显示那一行、不报空。
- **打开成功后关闭弹窗**：导航本身就是回答，且关闭会把焦点还给入口；只有打开**失败**（导航抛错）才保留弹窗并留一行痕。
- **行的结构**：行是 `<li>`，其可点面是一个真正 `<button>`（Enter/Space 免费可用），两个动作按钮是它的**兄弟**而不是嵌在它里面——`role="button"` 里嵌按钮是把交互内容套进交互内容；"焦点即浮现动作"由 `:focus-within` 承担，行为与设计一致。
- **当前工作区的取法**：设计没规定它从哪来。实现用"当前会话的工作区成员关系优先、其次 `cwd` 匹配"，两者都认不出时**回落到「全部工作区」**——按一个未知的工作区过滤会给出**假的**「暂无其他会话」。
- **列表上限 50**：设计只规定"有界必须标注"、没有给数；取 50（与 `team_link_list_sessions` 的 `LIST_LIMIT` 同量级）。
- **入口图标**用 seed 模块的 `IconLinkOutline16` / `IconLinkOutline14`（与【设置】行同一对尺寸）；设计只说「图标 + 文字」，未指定字形。
- **真机复验点 3（3080 页面上的宽态 / 收起态 / 标题栏收起态渲染，以及与消耗卡片、【设置】的相对位置）未执行**：本批只做单元面，客户端 bundle 需重建 + 刷新页面。与 0.3.8 及批次 1–2 同口径，**不把未验的说成已验**。

### 批次 4 —— 收口轮：闸门收窄 · 合成链路补投递 · `task-and-goal` 接数据源 · 列表丢弃当前会话 · 计数勘误 · 跨档同步

> 起因：一次**只读分歧审计**（对批次 1–3 的交付与文档做逐条对照）报出 10 条，本轮逐条落地其中可执行的部分。审计未改任何文件。

**修了什么**：

1. **A 能力闸门收窄到「自建继任者」支路**（审计第 5 条 / N1）：`reappoint` 的能力闸门原先在**动词入口**——无 `agents.create` 的宿主上，**连「改任给活成员」也一并被拒**（那是本批之前不存在的回归）。现在：无该服务 ⇒ **弹框照开，只是候选里不出现「自建继任者」**（只列活成员）；**仅当活成员候选也为 0** 时才 fail-closed 报告且**零弹框**。对话框里同步写明能力缺口（N1：只降级该面子面，并留痕）。
2. **B 恢复面文案与实现对齐**（审计第 3 条 / N3）：`recoveryBoundaryText()` 第 ③ 项由「候选由插件从**活成员**计算」改为「**活成员 ∪ 常驻的「自建继任者（新建会话）」**」；同批核过工具 `description` 的能力闸门段/合成链路段与 `recoveryDiagnosticLines` 的恢复入口行，三处一并改准。另修正交接正文 `mission` 节里一句**收窄之后就不成立**的话（原文「全队没有可改任的活成员」）。
3. **C 合成链路补投递**（审计第 9 条，功能性缺口）：`appointSelfBuiltSuccessor` 原先**止于审计留痕**——令牌铸了、文档写了、留痕三处齐全，**却从未交给刚建出的继任者**（对照 `successor:"auto"` 的结尾是 `handle.agent.followup(…)`，§11.4.5）⇒ 新建的继任者**空转**，30 分钟后被超时清扫取消。现在补上同一条投递（`followup`，不是 `inject`；消息 `source` 仍恰三成员），并在回执里如实报出投递结果（失败有自己的文案与出口）。
4. **D 五硬节的 `task-and-goal` 接数据源**（审计第 8 条）：`selfBuiltHandoffBody` 原先把该节**硬编码成「未知」**，而它算出的 goals 读数只落到 `in-flight` ⇒ **该节声明的数据源从未被消费**。现在该节渲染 goals 读数，**读不到才**标未知（§4.2 (c) ④）；`unknowns` 同步改为「读数覆盖不到的那一部分」，不再在读到读数时仍声称 goal 未知。
5. **E 计数勘误**（审计第 4 条，本仓红线）：批次 2 的红相明细**自相矛盾**——U9 记 10 但其中「新建的会话是根会话（meta 无血统字段）」在修复前**本来就为真**（旧实现从不 `create`，`creates` 为空 ⇒ 判据恒真），语义变更那一组列了 6 个名字却记 5 条，加总 22 与实测 21 差一条。**按重跑实测定档**为 U7 2 + U8 1 + U9 **9** + U10 1 + U11/收敛性 2 + 语义变更 **6** = **21**，计数与列表同一次编辑改准（`docs/verification-log.md` 批次 2 的「计数勘误」小节留原始读数与逐条名单）。
6. **F 跨档同步**（审计第 2、6 条 / 设计 §10 清单）：`docs/team-upgrade-design-2026-09-17.md` §9.1.3 增补**双服务 fail-closed 的挂载语义**（批次 1 要求做、当时未做）；`docs/collab-enhancements-design-2026-09-19.md` §11.9.5 ③ 的**条目本体**由旧规则（「候选由插件从 live 成员计算」）改为新规则，③ 的注解与 §11.9.4 增补第 2 条、验收表 U27 行同批对齐（同一条事实不再有两处口径）。
7. **G 客户端列表丢弃当前会话**（审计第 7 条 / owner 裁定 ③）：`visibleSessionRows` 现在**丢弃当前会话**——本面板的用途是「**其他**会话」（当前会话的复制/导出已在会话头部按钮上），把它列进来会让「暂无其他会话」这句话与实际行为不一致。副作用如实写明：官方那条「blank 行只在它是当前会话时保留」随之退化为「blank 行一律丢」。验收点：工作区里**只剩当前会话**时显示「暂无其他会话」且一行都不画。
8. **H 真机复验点 1**（审计第 10 条）：对运行中的 `127.0.0.1:3080` 重放了 §6 点名的三条请求 × 两种标记（无 token / `Host: evil.example:3080`），原始读数见 `docs/verification-log.md` 批次 4 · 客户端与文档收口一节的表。**结论：运行中的进程仍是修复前的宿主半边**（本插件路由在跨站标记下仍 200 + 5.48 MB 会话数据，而 `/` 与 `/open-in-app/apps` 同判 401/403）——批次 1 的栅栏**在树里，但需 DSH 重启才在真机生效**（本节开头的发布状态行早已如实声明）。故**真机点 1 的红相实测留证完成，绿相留待父侧在一次重启窗口里复跑同六条**。

**为什么**：A 是**回归**（闸门位置错，把不依赖该服务的那条路也拒了）；B/F 是**描述面与实现面分叉**（本仓 N3 红线）；C/D 是**功能缺口**（继任者收不到令牌 ⇒ 空转；一节声明的数据源从未实现）；E 是**计数与列表不一致**（本仓文档卫生红线）；G 是 owner 裁定 ③（列表口径）。

**怎么验证（变异验证）**：

- **修复前必红（宿主半边）**：隔离夹具 `.test-tmp/s1-red/`（**本轮测试文件** + `git show HEAD:lib/index.js` + 当前 `lib/client.js` + `README.md`，在夹具目录里跑）⇒ `node host-half.test.mjs` → **`927 (failed: 7)`**，7 条恰是本轮每一条判据，逐条读数：A 两条（`{"labels":[],"dialogs":0,"creates":0,"pending":null}` + 返回文案「本宿主没有可用的 agents.create……连确认框都不弹」= 整动词被拒）；C 三条（继任者 `followedup.length === 0`）；D 两条（`task-and-goal` 逐字为「**未知**」）。
- **修复前必红（浏览器半边）**：隔离夹具 = 当前 `client-half.test.mjs` + 当前 `lib/client.js` **只把 §4.3.2 的可见性那一行还原**（其余一字未动）⇒ **`259 (failed: 14)`**：其中 7 条是 G 的语义判据（核心读数 `{"empty":"","rows":["session-cur"]}`——修复前那一行真的被画出来了），另 7 条是**按行序号索引**的跟随断言（列表从 3 行变 2 行，索引随事实走）。
- **修复后全绿**：`node host-half.test.mjs` → **`927 (failed: 0)`**（净 +7 条断言，其中 1 条是改写旧 U10）；`node client-half.test.mjs` → **`259 (failed: 0)`**（基线 920 / 257；浏览器半边**净 +2 条**断言、另**改写 10 条**既有断言的表达式来跟上「列表 3 行变 2 行」这条事实）。
- **E 的实跑重算**：批次 2 的红相用原取法重跑（`.test-tmp/s2-red/` = `git show 8d40343:host-half.test.mjs` + `git show b48b34e:lib/index.js` + `git show 8d40343:lib/client.js` + `git show 8d40343:README.md`）⇒ `920 (failed: 21)`，并逐条点名了那 21 条 FAIL（U9 组里那条血统字段断言实测为 `PASS`）。

**如实标注（设计未逐字规定、由实现定夺的地方）**：

- **D 的「读到读数」那一支在生产里不可达**：本动词的前提就是前任**没有活动代理**，而 goals 按 agent 取读数 ⇒ `ctx.agents.get(incumbent)` 必为 `undefined`。这一支是按 §4.2 (c) ④ 的契约实现的（「读不到**才**标未知」），判据直接驱动**真函数**（与既有 U9 交接正文断言同一条纪律）。
- **投递在合成链路里的位置**：作为第 ⑧ 步排在审计留痕之后；§4.2 (c) 的编号是元素清单不是时序，`successor:"auto"` 的顺序是 prepare → 清 pending → followup，两种排法都不违反「令牌落盘之后才投递」。
- **真机复验点 1 的绿相未闭合**（需重启，本轮不重启）；**真机复验点 3**（收起态 / 标题栏收起态）仍未在真机核对，与批次 3 同口径留给父侧。

**同批同步的面**：`README.md`（§二 恢复段「选自建继任者时发生什么」、§4.3 数据源行、tests 徽章 927+259、§十 当前读数）；`docs/team-upgrade-design-2026-09-17.md` §9.1.3；`docs/collab-enhancements-design-2026-09-19.md` §11.9.4 增补第 2 条 / §11.9.5 ③ 及其注解 / U27 行；`docs/verification-log.md`（批次 2 计数勘误 + 批次 4 两小节）；本条目。
---

## 0.3.8 — 2026-09-20 — ① 发送方可见性（§10.1）· ② `/team_session` 自动建队（§10.2）· ③ 自动换届交接（§11）

> ① 让**发送方自己**也看到自己发出的跨会话消息卡片（此前只有接收方有卡片，发送方只看到一行藏在可折叠工具树里的灰字）。② 一条命令建 N 个 worker 根会话并登记进 roster。③ 让换届能自动建继任者并交接，另加一个**两动词、attended-only** 的团队恢复工具与活性诊断面。设计与裁决记录：`docs/collab-enhancements-design-2026-09-19.md` §10.1 / §10.2 / §11 / **§12（真机验证结果）**、`docs/consult-minutes/2026-09-19-consult-37-minutes.md`、`2026-09-20-consult-43-minutes.md`。
>
> **发布状态**：三项的宿主半边都需 DSH 重启才在真机生效。**演练 8（①）已通过真机验证**（§12.1）；**演练 9（②）/ 演练 10（③）/ H1 / H3** 待另一次重启窗口（DEFECT-1/2 的修复要重启才生效，见 §12.4）。

### ✨ 新增
- **A：`tool.call.toolview`（key 逐字 `team_link_send`）**——发送方的工具行从通用灰行变成与接收方同款的出站卡片。**逐目标行 = 目标身份 + `outcome` 短句 + （忙碌时）徽标**：`outcome` → 固定短语（走 locale，zh/en 两套键），**不再原样搬运模型可见的 `detail` 报告句**——那句含「已投递到 …」「目标处于空闲」「steer 注入当前回合」这类**投递机制电报**，让人看的卡片偏机制而非结论且一个目标占 2–3 行（例：80 字符 → 44 字符）；`detail` **仍留在 meta 里**（模型可见文本的事实源 + 降级兜底），只出现在**模型可见的报告**中。用户 2026-09-20 决定，见设计 §10.1.5 / §12.5。
- **D：自有 conversation node definition（kind `team-link-send`）+ 顶层摘要卡**——在会话流**顶层**多一条「发给谁 / 正文 / 汇总计数 / 时间」。**不写任何日志事件、不动模型上下文**。
- **数据链：`output.presentationMeta` → `tool/result.meta`**——卡片读**结构化回执**，不再 regex 解析工具返回文本；持久化后可回放重建同一张卡。
- **② `/team_session`（设计 §10.2）**——一条命令建 N 个 worker **根会话**（N ≤ 8、每队成员 ≤ 24，两个**代码常量**，刻意不进 settings schema）→ 逐个 create 后 `followup` 投递启动任务 → 按 role 幂等登记进 roster → 与主会话建立 pairs 双向免确认通道。批量动作前有**一次**写明「数量 / 模型 / 预设 / cwd / 保守成本口径 / 将建立的信任」的确认框，**取消即零创建零 pairs**，无确认服务即 fail-closed。命令走**可选** `ctx.inject(["commands"])`（模块级 `inject` 仍 4 项），**服务缺席 / 迟到 / 无 `register()`** 三种降级都只丢这条命令。会话 `meta` 恰 `{cwd, agentPreset}`（`origin` / `parentSession` / `delegationDepth` / `parentAgent` 一律不写 ⇒ 根会话），`AgentHandle` 由**插件根 ctx** 的控制器持有。
- **② schema 新增（唯一一处，已裁定）**：`PolicyConfig.pendingCreates`——§10.2.6 的 pending-create 意图必须持久化，否则重启后启动清扫扫不到任何东西。§10.3 原文「schema 均不改」措辞过宽，已改准为「**既有 key 的语义与形状不改；新增须为设计明确要求的闭环所需并在文档与 CHANGELOG 记录**」；既有八个 key 的名字、形状与语义一字未动。
- **② 生命周期（设计 §10.2.5，设计要求必须写进文档）**：**插件卸载/重载 = 全队 teardown**——`AgentHandle` 由插件持有，拆插件即拆掉这些代理，**而会话仍在盘上**。这不是事故，是生命周期事实。恢复路径三步、不新增机制：`team_link_list_sessions` 如实读成 `✕ 未运行` + `verdict=dead` → 在侧边栏**逐个打开**把会话拉回活的代理 → 用 `team_link_roster action=set-role` 重新登记。`/team_session` 的返回文案也带同一条提示。README 的 ② 段落与本条同批补齐。

### ✨ 新增（③ 自动换届交接 · 设计 §11）
- **③a 主路径**：`team_link_rotate action=prepare` 新增 **`successor:"auto"`**（+ 可选 `handoff` 正文）——插件自建**根会话**续任、写**三层交接文档**（头 YAML / 事实段（**与 claim 返回文案同一事实源**）/ 正文 **5 硬节** `mission`·`in-flight`·`commitments`·`unknowns`·`task-and-goal`），再把令牌与正文 **followup** 投给继任者、由它自行 **claim**（**claim 逐字未动**，未新增令牌类型）。**缺项阶梯**：auto + 空正文 → **拒绝于工具入口（零建会话 / 零令牌 / 零 freeze）**；硬节缺 → 拒绝并点名；软节缺 → 放行 + 警告；显式 successor 无正文 → 放行 + 警告；文档写失败 → **abort-before-prepare**。便捷命令 **`/team_rotate <role>`**（走可选 `commands` 注入，**只对现任开放**）。
- **③b 恢复工具 `team_link_recover`**——**恰两个封闭动词**：`revive`（复活**同一**会话：身份 / roster / 信任**零改动**；**只对插件自建会话**，人类自建只输出深链指引）与 `reappoint`（**人类对话授权的 prepare**：候选由**插件**从 live 成员算出、**模型不得指定** → 逐字走既有 prepare + claim）。**attended-only：刻意不设 provisional / 无人值守变体**（pair 迁移可被 sweep 自动回退，**incumbency 不可**），无确认服务 → fail-closed。**八条反后门约束全部落成断言**：封闭动词 / 候选插件算 / `revive` 只绑当前 `current` / `writerGate` 原样不动 / **绝不把 `writer` 降级为 `any`** / 限速 + 三处留痕 / 进入先跑过期清扫 / TOCTOU 双复检（对话框弹出时与落笔前各重查活性）。
- **③b 活性诊断面**：三道门（`writerGate` / `rotateGate` / `retireGate`）**本体仍是纯函数**，由**有 ctx 的工具层**富化拒绝文案（旧文案对死现任是误导性的）；`roster get` 现任行加注记；启动清扫新增「current 无活代理的角色」一行；**`roster.md` 刻意不加**（落盘文件不烙活性读数）。两个派生词 `vacant`（刻意空缺，`current=null`）与 `seated-dead`（悬空指针）**不落盘**。

### 🐞 真机验证暴露的三个缺陷（均已修，待复验窗口）
- **DEFECT-1（高）**：编程创建的会话**跑不起来**——`prompt variable "{{model}}" has no value for this assembly (section "deployment:persona-prefix")`。根因：`buildTeamSessionCreateOptions` 只在给了 `preset=` 时才写 `meta.agentPreset` 并 mount ⇒ **缺省时新建 agent 没有 persona-prefix 组装源**；而官方模板 `createWebhookSession` 是「**总是** resolve（缺省也解析）并 mount」。修法：对齐模板（缺省也解析 + 无条件 mount；服务缺席降级为一行 warn 且不阻断创建）。**H4 假设由此被真机推翻**（**会建 ≠ 能用**）。
- **DEFECT-2（中高）**：编程创建的会话**未挂进工作区** ⇒ 用户侧边栏看不到（须手动切工作区）。根因：模板在 `agents.create` 后调 `await workspace.attachSession(sessionId)`，我们**全库 0 处**。
- **两个缺陷是同一个模式**：**模板有一组「创建后必须做的事」，实现只做了一部分**（漏了 preset 解析/挂载、漏了 workspace 挂载）。
- **DEFECT-3（高）**：**DEFECT-1 只修了一半**——补上了 preset（组装源），却**没给装配里引用的 `{{model}}` 赋值**。**机制证明**：`{{model}}` 取的是 **agent 自己的 `options.model`**（`dsh-agent-loop/lib/index.js:1534` 的 `ctx.systemPrompt.variable("model", (context) => context.agent?.options.model)`）⇒ **宿主缺省根本不在那条路径上**；而 `buildTeamSessionCreateOptions` 在未给 model 时让 `agentOptions` 为空、`installTeamSessionModelSelection` 首行返回 ⇒ 编程创建的 agent **没有任何模型选择** ⇒ 首回合报 `prompt variable "{{model}}" has no value for this assembly (section "deployment:persona-prefix")`。**这一处「故意不做」是父侧批准的**（理由写的是「缺省时宿主自己的 defaultModel 已生效」）——**真机把它推翻了**。
  **修法**：对齐模板 `resolveRequest:30-36`——未给（或**只给了一半**）时用 `ctx.agentDefaultModel.currentSelection()` **解析/补齐**并写进 `agentOptions`，再把同一份选择交给 `installTeamSessionModelSelection`；服务缺席 ⇒ **拒绝创建**（fail-visible——宁可不建，也不建一个跑不起来的会话）。**② 与 ③a 两条路径各自断言**（`successor:"auto"` 建的继任者同样受影响，而那是**信任迁移路径**）。
  **教训（已写进设计 §10.2.2）**：凡列入「故意不做」清单的条目都是**判断**，必须**在真机上被证伪过**、否则显式标注为「未验证的假设」。**「我认为宿主会兜底」不是判据。** DEFECT-1 与 DEFECT-3 是**同一份清单上的同一次失误的两次爆发**。
- **DEFECT-4（中）**：编程创建的会话**全部叫工作区名**（真机原话：「会话名称都是 `dsh-session-link-pro`」）⇒ 侧边栏里**互相无法区分**。根因：把官方模板第 ⑧ 步 `sessionTitle.rename` 判成了「不做」，理由是「命名是**用户可见的交互决定**、设计没给就不自造」。**该理由不成立**——**不设标题不等于不替用户决定**：**默认值（工作区名）本身就是一次很糟的决定**。修法：按**已有的结构化信息**派生可区分的默认标题（`<team> · <role>`；只有 team ⇒ `<team>`；否则会话 id 前缀；**不读 cwd、不造随机数**），走 `ctx.get("sessionTitle")`；服务缺席或 rename 抛错 ⇒ **一行具名 warn、不阻断创建**——与 preset/模型选择那两处的 fail-fast **口径不同且是有意的**：那两处决定会话**能不能跑**，标题只决定它**长什么样**。**② 与 ③a 两条路径同批**（注入点仍只有一处）。判据含一条**可区分性**：同一批里不同 role 的标题**两两不同**。
  **派生边界（同日补）**：上游 `dsh-session-title` 按 `maxTitleBytes: 80`（字节）**剪尾巴、不追加标记、不拒绝**，而团队名只受 `[a-z0-9-]+` 约束、**没有长度上限** ⇒ 原样交出会**把 role 段剪没**、同队两 role 撞名（实测 `distinct: 1 of 2`）⇒ **团队段先截、role 段完整**（UTF-8 安全截断 + 省略号；团队段连前缀都放不住才退成「只有 role」，**不是**工作区名、也不留空的 `<team> · `）；**未给团队名新增任何长度约束**。边界如实：role 自身 >76 字节时任何方案都装不下，此时给非空省略号前缀。
- **无人值守 `claim` 的读数歧义（真机观测到、同日修）**：真机上 `claim` 的确认框**无人应答**（外层工具桥在 ~118.5s 以 `AbortError/ABORTED` 结算），**调用方只读到「tool call aborted」**，**而变更其实已经提交**（落在设计规定的「全部 provisional 迁移」分支）。**根因**：`abort` 被当成「对话框**失败**」（`timedOut` 只由插件自己的 3 分钟计时器置位）⇒ **超时与失败在读数上完全同形、后果却不同**（已换届 vs 未换届）。**修法（只改读数，不动 attended-only 本体）**：`ROTATION_DIALOG_CAUSES`（`unavailable`/`no-answer`/`timeout`/`failed`）+ `dialogNoAnswerError(error, signal)`（把 abort/timeout 归入「**无人应答**」，`signal.aborted` 是权威半）+ `unattendedClaimReading(...)`（**唯一**渲染点，`provisionalGuidance` 复用）；超时读数**自报真实后果**（未获应答 / **这不是「调用失败」** / 已按设计以 provisional 迁移**并已落盘** / 重试或等待都不会撤销它 / **具体到期时刻** / **批准路径**＝人类在设置 UI 置 `provisional: false`）。**真正的失败路径（无确认服务 fail-closed / 令牌无效 / 域外 / 写失败）文案逐字未改**，并由断言互不混淆地钉住；`team_link_rotate` 的 claim 描述另补「超时不是失败」，使调用方**调用前**即可读到。
- **教训（已写进设计 §12）**：① **模板引用必须带「时序清单」**，不能只引形状——§10.2.2 只写了 `meta` 的形状，实现逐字写对却漏了后续步骤；② **断言可能成为缺陷的同谋**——修复删掉的那条旧断言「无 `agentPresets` ⇒ 会话照常可用」正把缺陷固化成了期望行为；③ **探针的前置条件本身也要被验证**——H1 默认「会话已经能被看到」，而真机暴露的正是它根本不在该工作区的列表里。

### 🔧 修复（差异审计与代码评审发现）
- **F2（仓库自身良构红线）**：`targets[].sessionId` / `expr` / `senderSessionId` / 信封 `type|pri|ref` **未经 `wellFormed`**——会把孤立代理项写进会话日志。已全部过闸 + 6 条回归锁（此前代码注释与 README 的「永远良构」自述**不成立**，已改）。
- **F1**：A/D 两面曾逐字重复 head/body/summary/foot。已按「每块信息只准出现一次」分工（D = 标题+时间+正文+汇总；A = 标签+逐目标行）。
- **F3**：客户端**模块级** inject 新增 `uiConversation`，会把设计承诺的「只不渲染」放大成「整个客户端半边不加载」。已改回 3 项 + 动态注入。
- **B3 / 评审 #3**：四条客户端注册全部过 `guardedSlot`（一条抛错不影响其余）。
- **评审 #1**：文件头注释曾称空闲目标「queued without being woken (inject)」，实际是 `followup`；已改。
- **评审 #2**：客户端对 `targets` 行数设界（24）+ 显式截断标注（宿主侧同源缺口见下）。
- **差异审计修复轮 🟡-1（本轮的实质 bug）**：**启动窗口内的 pending-create 意图在并入设置命名空间时被丢掉**。起因是同源形状：许可「整份写入当前命名空间」的谓词 `policyIsAtDefaults` 在 ② 轮加上了 `pendingCreates`，而**兄弟写面**——`adoptMemoryWindow` 交给 `update()` 的那笔补丁——仍是先前七个 key（文件自己的注释就写着「Fields added to DEFAULT_POLICY belong here in the same change」，写面没跟）。后果：窗口内写下的 §10.2.6 意图并入后消失，**下一次启动的清扫清单里就没有这条本该交还给人的孤儿记录**（审计插桩读到 `pendingCreates=undefined`）。修法：补上第八个 key，并在谓词与写面的注释里把「两半必须同批同名」写死。**教训与 ① 的跨轮缺口同源**：新 key 加进了谓词，漏了兄弟写面。
- **差异审计修复轮 🟡-2（描述与实现不符）**：README 的 ② 段落此前声称本轮修掉了一个「旧命名空间迁移会**把当前命名空间整片抹掉**」的缺陷。审计逐行核对基线后判定**该机制不成立**：写**当前**命名空间的那笔补丁基线**就已只含**那五个信任字段，写「整份 `DEFAULT_POLICY`」的是**旧命名空间**那次重置（语义是「重置旧命名空间使迁移不重复」），且 `dsh-settings` 的 `update()` 是 **patch 合并**（另有独立 `replace`），缺 `teams` 的补丁不可能抹掉 roster。已把 README 改写成实际发生的事（谓词与写面覆盖新增的 `pendingCreates`），并为该改动补上断言与条目。
- **差异审计修复轮 🔵-1（证据措辞）**：此前 CHANGELOG / README / 测试注释称「插件对会话只有 `sessionQuery` 的三个读方法，故可静态证明无写入面」。实测是**四个**读方法（漏了 `listSessions`），且插件**确有**会话写入面（`ctx.agents.create` / `agent.followup`）。红线仍成立，但理由换成诚实的版本：那两条既有路径用的是**上游定义**的事件类型，本插件无从发明新类型；**静态正则证明不了这一点**——它锁的是「本模块不得自己长出写入面」（审计变异 M6：往模块里放一个日志写入 API → 1 红），运行时那条读的是**桩**、结构上观察不到新事件类型。断言本身保留，全部措辞已改。
- **差异审计修复轮 🔵-2（有意保留的偏差，已写入代码注释）**：§10.2.3 的模板是 `createUserMessage({content, source})`，实现是手写字面量。裁定：**保持手工构造**——`createUserMessage` 来自 `@deepseek-ai/dsh-llm`，而本模块导入面是**六项白名单**且经 U19 锁住；两条消息实体逐字相同，差别只在上游构造函数产出被**深冻结**（`brandString` 是恒等函数）。不为一行深冻结把导入面扩到 7 项。理由与该回退条件已写进 `teamSessionKickoffMessage` 的注释。
- **差异审计修复轮 🔵-3（状态行/条目不同步）**：README 的设计档索引行仍写「② `/team_session` 自动建队（未实施）」——已改为**已实现**（并标注与 ① 同样「未发布、未真机未验证」）；本小节此前标题只写「发送方可见性」且**没有 ② 的任何条目**——已补。
- **差异审计修复轮 🔵-4（计数）**：U19 一节的断言数是 **17**（此前写 16）；「② 收口轮的 WIP 实际是 640」被实推翻——那次 WIP 提交自带的 README 记的是 **639**（`506 + 133`，内部自洽），640 只是 `656 − 16` 的算术推演。本文件的计数一律以套件自报的那两行为准。

### 🧱 宿主侧行数界（设计 §10.1.2 修正后补）
- `buildSendCard` 对 `targets` 封顶 **24 行**，并写入 `targetsTruncated: {shown,total}`；裁的是**呈现**——`summary` 计数与文本报告仍**全量**（卡是有界呈现，报告是全量档案）。
- 背景：输入侧的 ≤8 只约束**表达式**，而一个 `team:<n>/*` 会在宿主侧展开成**全队成员的行**——所以「持久化卡片必须有界」这条原设计**在宿主侧并不成立**，本轮补上。

### 🔗 跨轮交互缺口（已定位并修复）
- **症状**：宿主先裁到 24 行并写 `targetsTruncated`，而客户端判超限是「自己数行数 > 24」——对**自产卡永远不触发**：A 面看不到截断标注，标签显示的是**已画出的 24 行**而不是真值 30。属**静默失真**（把有界呈现当成全量呈现）。
- **怎么发现的**：实施方在下一轮收尾时**主动对照自己上一轮的成果**上报；父代理用 `grep targetsTruncated lib/client.js`（**0 命中**）独立复现。两轮各自都正确，缺口只存在于两轮的**交互**里。
- **修法**：客户端新增 `readTargetsTruncated`（坏可选成员只丢该成员，不否决整张卡）+ `sendCardTargetTotal`（有标记取 `total`，无标记取实际行数）；A 面**两类超限都出标注**；标注里的 `{shown}` 取**实际画出的行数**——不采信外来 `mark.shown`，否则一张手改卡会在画着 24 行时宣称「仅显示前 5 行」。设计 §10.1.2 / U14 已把这条契约写死。

### 🧾 本期后续轮次（③ 的审计修复 · 真机缺陷 · A 面改人话）
- **③a 差异审计的修复轮**（审计 0 🔴 / 6 🟡 / 7 🔵）：令牌过期时的「额外报告插件自建继任者」**不再依赖内存态 handle**——那个判据在插件重载后恒为假，**恰是设计要防的「没人知道的孤儿」窗口里点名行静默消失**；改为**持久证据优先**（交接文档头部的 `successor:` 行 → 落盘的 `pending-create` 意图 → `hasHandle` 降为附加佐证），且点名行**自报判据来源**。同批：五硬节名收成**一个真源**（四处任一处改名而其余不跟**必红**）；命令键表改成**双向行为锁**（不在广告里的键必须被解析器拒绝）；改名不再让套件崩溃而是**干净地红**（并顺带发现另三处同类）。
- **③b 差异审计的修复轮**（审计 0 🔴 / 7 🟡 / 8 🔵）：**发起域落地**（{该角色最近一任前任} ∪ {团队现任成员}；域外拒绝**零副作用**，连确认框都不弹）；工具描述改准（`revive` 仅 coordinator、`reappoint` 任意角色——**授权从不源自 coordinator 身份**）；**两处空锁改成行为锁**（删掉身份复检 / 删掉入口过期清扫，都必须红）；补 `?? []` 护栏并收口 9 处深链访问（**任何 break 之后套件都能跑到底并打印 `assertion total`**）；死代码合并、候选对话框改**单选**（原多选却只取第一位 = 静默丢弃）、多处注释里一句**被实测推翻的信念**改准（schemastery 不 strip 未声明字段）。
- **合并代码评审（PASS：1 🟡 + 3 🔵）的收尾修复**：① **`revive` 侧的身份复检从「半空锁」落成与 `reappoint` 逐字对称的行为锁**（`Y3-sym` / `Y7-sym`，两条互不顶替）——**红相实测**：把该复检整段注释掉 → `835 (failed: 1)`，**唯一红的就是新锁、所有既有 revive 断言仍绿**，这正是「半空锁」的实证；② 四处机械拼接的排版残留清理，附**三层「纯空白」证明**（正则命中 4 → 0、逐处空白剥离后逐字相等、**全文件字节账** `478574（起始）+ 8（本项插入的 4 个 CRLF）+ 951（#4 那段净差）= 479533 = 实测字节数`）；③ `autoHandover` 的事实段计数改为**确认之后、写文档之前重读一次**再算（使「文档里的数目 == `prepare` 即将快照的那份状态」），并加行为断言（对话框期间往 pairs 加一条 ⇒ 文档必须写 **4 条**而非 3 条；删掉重读即回落成 3 ⇒ 红）。
- **A 面改从结构化字段渲染一句人话**（用户决定）：A 面**不再原样搬运模型可见的报告句**（含「已投递到 …」「目标处于空闲」「steer 注入当前回合」这类**投递机制电报**，让人看的卡片偏机制而非结论，且一个目标就占 2–3 行），改由 `outcome` / `busy` / 目标身份渲染短句；`detail` **仍留在 meta 里**（模型可见文本的事实源 + 降级兜底）。原「卡内行 == 报告首行」的字符串等式锁**作废**，换成**跨半边行为锁**（新增 `outcome` 枚举值而无客户端短语 → 必红）。

### 🧪 验证
- `node host-half.test.mjs` → **874**（failed: 0）；`node client-half.test.mjs` → **146**（failed: 0）（**0.3.8 收口时点**；合计 **1020**）。基线演进：host 506 → … → 675 → 831 → 848 → 855 → 868 → **874**；client 46 → … → 138 → **146**。
  > **计数口径**：一律以套件**自报的那两行**为准；本文件历轮的时点计数（如 675 / 813）保留原文不改，最新值见本行与 README 徽章、第十节。
- **U19 红线回归**（② 收口轮补，**17 条**断言——此前误记为 16，差异审计修复轮 🔵-4 已改；判据是在 U19 小节内逐个数 `check(` 调用点（修复轮**之外**另加的 7 条见下一条，两者不要相加））：① 无新增日志事件类型；② 投递 `source` 恰三成员；③ 模块级 `inject` 仍 4 项；④ 既有 schema 形状与投递双门零改动（含「整批恰弹一次对话框」）。① 的判据经差异审计修复轮 🔵-1 改写为**诚实版**：插件对会话的写入面**存在**（`ctx.agents.create` / `agent.followup`），`sessionQuery` 是**四个**读方法（`listSessions` / `readSession` / `readSurface` / `readTitleSnapshots`），所以那条源码断言锁的是「**本模块不得自己长出写入面**」（审计变异 M6：往模块里放一个日志写入 API → 1 红），而**不是**「静态正则证明了事件类型」——那两条路径的事件类型由**上游**定义，正则证明不了这一点；运行时那条读的是**桩**，结构上观察不到新事件类型，是旁证而非判据。
- **并发纪律**：§10.2.6 要求 create 与 followup 串行（或 ≤2）——实测**已在树上**（单条 `await` 串行循环、`agents.create(` 全模块恰一调用点），故未新增队列，只补断言：提供方侧在飞峰值 **实测 1 ≤ 2**。
- **schema 新增（唯一一处，已裁定）**：`PolicyConfig.pendingCreates`——§10.2.6 的 pending-create 意图必须持久化，否则重启后启动清扫扫不到任何东西。§10.3 原文「schema 均不改」措辞过宽，已改准为「**既有 key 的语义与形状不改；新增须为设计明确要求的闭环所需并在文档与 CHANGELOG 记录**」；既有八个 key 一个字未动。**差异审计修复轮 🟡-1 让这条从「声明」变成「被断言的行为」**：谓词与它许可的整份写入如今都点满八个 key，并入窗口时八个一起写。
- 变异证据（单点回退，修复前必红）：正文截断去掉 → 5 红；结构化 busy 去掉 → 3 红；槽位 key 改近形 → 3 红；不读回执 → 12 红；窗口截断回退去掉 → 2 红；**F2** 的 `sessionId` / `expr` 孤立代理项 → 各 1 红；**评审 #2** 渲染上限去掉 → 2 红；**评审 #3** header 注册去掉护栏 → 2 红；**宿主行数界**去掉 → 3 红；**客户端读标注**路径去掉 → 3 红；**🟡-1 的写面那一行**（`pendingCreates: [...memory.pendingCreates]` 去掉）→ 1 红。
- **差异审计修复轮（本轮实测，红→绿）**：新增 7 条断言（`U9 对照 (pendingCreates)` 一组）。**红相**：先插断言、`lib/index.js` 的写面**一字未改**跑一次 → `663 (failed: 3)`，三条同时红（「窗口内的意图并入后仍在」「并入的行逐字段完整」「并入恰是那八个 key」），而同一组的三条前置断言当场全绿（两个 provider 都迟到 ⇒ 整批在内存窗口内跑；commands 迟到挂载不额外留行；worker-b 的 create 失败 ⇒ 意图未被回填）——前置全绿是**关键对照**：它证明红色不是「路径没走到」。**绿相**：把 `pendingCreates` 补进 `adoptMemoryWindow` 的补丁后 → `663 (failed: 0)`（宿主侧唯一改动，客户端 `138 (failed: 0)` 同期无损）。审计侧另有独立复现（`%TEMP%` 副本插探针：`AUDIT after fold: teams=["night-shift"] pendingCreates=undefined`）。
- **② 收口轮的 WIP 断言数**：**639**（`506 + 133`，那次 WIP 提交自带的 README 记的就是 639，内部自洽）。此前「WIP 实际是 640」的说法是 `656 − 16` 的算术推演，已作废（差异审计修复轮 🔵-4）。
- **代码评审 round-1 收口轮（文法面，红→绿）**：评审发现 `/team_session` 的**位置参数（bare token）被解析却从未被消费**——而 `description` / `input.hint` / 解析器注释 / **plan 自己的报错文案**都在宣传这条路径，用户照报错提示输入仍会得到同一个报错；同时 `task=` 与自身注释矛盾（按空白切分 ⇒ 未加引号的多词任务把词**静默丢进被忽略的 `bare`**，而 `task="…"` 又把引号留进值里）。**修复**：位置参数真正折进 `roles`（`team=` 仍是**必需** key——「首个裸 token 兼作团队名」与「用户忘了 `team=`」无法区分，猜错的代价是把会话登记到别的团队名下）；`task=` 改为读到**下一个已知 `key=`**（保住既有 `task=做接口 model=… preset=…` 次序），`key="value"` 统一剥引号、**半引号一律拒绝**。**红相**（拿 `HEAD:lib/index.js` 原始字节对比）：修复前 `roles=undefined` / `task="fix"` / `bare=["the","bug"]` / `team="t"` 带引号；修复后 `roles=["worker-a","worker-b"]` / `task="fix the bug"` / `bare=[]`。**端到端**（真跑命令，不是只查解析层）：`team=night-shift worker-a worker-b task=fix the bug` → 创 2 会话、roster 落 `coordinator,worker-a,worker-b`、kickoff 正文含完整 `fix the bug`；缺 `team=` 时明确报错。**绿相**：`675 (failed: 0)`（新增 11 条）。同批：README 架构图「11 个工具」→「**8 个工具 + 1 条 `/team_session` 命令**」；死代码 helper `plannedId` 改为**真按 role 查**并投入使用（另删同一处零引用的死常量 `TEAM_SESSION_ROLES`）。
- **真机验证**：**演练 8（①）已通过**（2026-09-20，证据见设计 §12.1：A 面是卡片、D 面顶层摘要卡存在、F1 信息分工成立、导出 3611 事件无自创类型且 `source` 恰三成员）；宿主侧其余改动需 DSH 重启生效 ⇒ **演练 9 / 演练 10 / H1 / H3** 排在用户批准的**下一次重启窗口**（§12.4）。另：真机还暴露并修复了两个缺陷（DEFECT-1/2），其修复同样**需重启才生效**。

---

## 0.3.7 — 2026-09-18

> 收尾修复轮。修两个在**真实部署中实测到**的功能性阻塞，并收掉一轮代码评审的分歧。设计与裁决记录：`docs/team-upgrade-design-2026-09-17.md` §9、`docs/consult-minutes/2026-09-18-consult-36-minutes.md`。

### 🔴 ① settings 持久化静默失效：团队状态从不落盘

**症状**（真实部署实测）：`team_link_watch register` 返回成功、`list` 也能看到条目，但 `profile/settings.yaml` 的 mtime 与全文**毫无变化**；日志里也没有任何本插件的 warn。后果：teams / watchdogs / pairs / rotation **全部只活在进程内存里**，每次 DSH 重启清零。盘上从来没有 `team-link:` 段——也就是说**自 0.2.x 起信任数据从未落盘**，「改名迁移已完成」这个当时写进交付报告的结论是错的。

**根因**（源码链）：cordis 的 `ctx.get(name, strict = true)` 只返回**提供方 fiber 已 active** 的服务（`cordis/lib/index.js:762-771`：`if (strict && impl.fiber.state !== 2) return;`），而 settings provider 要先完成 `[Service.init]`（读盘 → publish）才 active。插件却在 `apply` 期**一次性**取服务，取不到就静默退回内存引擎——此后**无重试、无重绑定、无日志**（只有 `register` 抛错那条路径才 warn，而日志里从未出现该 warn，故该路径也被排除）。

**修法**（三件套）：

1. **快路**：立刻试一次 `ctx.get("settings")`——提供方已 active（或同步 stub）时零额外延迟挂载，既有测试语义不变；
2. **可选有序注入**：没拿到就留**一行 warn**，并登记 `ctx.inject(["settings"], cb)`——provider 转为 active 时回调 attach。它**不是硬依赖**（服务永远不出现时插件照常加载并降级）；`ctx.inject` 本身不可用时再留第二行 warn；
3. **运行期惰性重试**：`get()` / `update()` 每次发现未挂载就再试一次；成功即挂载并记一行 info（`policy store attached to settings namespace "team-link"`）。失败**不重复告警**——「未挂载告警」有**两条到达路径**（激活时未 active / active 但 `register()` 抛错），二者共用同一个一次性门，故**每个未挂载窗口有且仅有一行 warn**。

**同类第二处一并修**：`registerExportRoute` 原先同样在 apply 期取 `ctx.get("webServer")` 时点快照（它当时能用，只因 webServer 恰好先于本插件 active——日志里从未出现该处 warn 即为此反证）。改用同一「晚挂 + 重试」模式，**降级语义一字不变**：始终没有 webServer 就只有一行 warn、头部导出按钮不可用，而 `team_link_export` 工具照常工作。

**启动窗口的数据一致性**（防御性冗余）：attach 之前 `update()` 只能写进进程内存，而这个窗口理论上不可达。真发生时，attach 会把这些内存写入按与改名迁移同形的规则并入设置命名空间（**仅当**设置侧仍是默认值——settings 始终是事实源），并留一行 warn，不静默丢写；并入**先于**改名迁移执行，迁移的「当前命名空间已在用」判据因此看到最终状态。

**明确不采用**的做法：把 `settings` / `webServer` 加进 `inject` 数组：`inject` 是**硬依赖**，依赖缺失时 cordis 令整个插件 fiber 不激活，与「服务缺失时插件完整降级」这条红线相抵；而 `ctx.inject` 与 `inject` 在确定性上等价（同样等 provider 完成 `[Service.init]`），故取零功能回归者。

### 🔴 ② 建队引导死锁：团队建了却永远写不进首任协调者

**症状**：模型能成功 `upsert-team` 建出团队，但**永远**设不进首任协调者，团队到手即只读，M2–M4 全部功能不可达。

**根因**：`upsert-team` 的**创建**路径本就不该过写权限门（否则没人能建第一个团队），但 `set-role` **必然**过门——而门在「`writer=coordinator` 且现任空缺」时**拒绝一切会话路径**。于是创建路径与写权限门之间形成了一个死锁。工具文案当时把用户推向「设置 UI」，但插件并没有提供该设置段，人路径实际是手改 `settings.yaml`——而 ① 又让手改同样读不到。

**修法**：`upsert-team` 在**创建**路径把**调用会话**播种为该团队的 coordinator 现任（经 `roleRecord` 产出规范形状，`history` 记「创建者自举」）。已存在团队的 `roles` 一律不改（幂等契约与「非现任不能借 upsert-team 劫持」同时保住），**不新增任何工具参数**（种子只来自 `exec.agent.id`），`writerGate` / `retireGate` **一行未动**——**手写**的空缺行仍然全拒并指向设置 UI（那是用户显式表达的状态）。

### 配套修复

- **导出文件名安全不变式**：web 导出路由对 `sessionId` 有净化（`replace(/[^\w.-]/gu, "_")`），而 `exportSession` 把原始 id 直接拼进产物路径。抽成共享 helper `fileSafeSessionId()`，两条路径复用。
- **旧命名空间 register 失败必须留痕**：原先 `catch { legacyScope = null; }` 静默吞掉——best-effort ≠ 静默，现在留一行 warn 点名旧命名空间不可用、旧数据不会自动迁移。
- **两个记录项**：`host-half.test.mjs` 结尾输出断言总数（README 计数从此可对跑）；`team_link_list_sessions` 的 provisional 计数与投递侧口径对齐（过期记录不再计入「provisional 配对 N 条」——按投递侧判据它已不再是通道）。

### 代码评审加固（同日追加，仍属 0.3.7）

- **provider 生命周期归属**：晚挂拿到的 scope 与其**注入 fiber 同生共死**（`owner.effect(() => () => detach(), …)`，与 webServer 站点 `target.effect(…)` 同一条规则）。provider fiber 被 dispose 时 scope 归零并记一行 info，store 回到未挂载态、provider 回归时由惰性重试重挂。此前只有捕获没有归属：scope 非 null 却已死，`update()` 每次抛错、而 `get()` **静默改答陈旧内存**。
- **一次性门的单位是「窗口」**：`detach()` 同时**复位**该门——否则第二个未挂载窗口里的**拒绝**会零告警（与 ① 的原始缺陷同形）。窗口**内**的惰性重试仍被同一门挡住，不会成为告警风暴。
- **事后链留痕**：attach 之后的（内存窗口并入 → 改名迁移）两步各自返回结论，统一记一行 info（`post-attach policy chain finished — …`）；`migrateLegacyPolicy` 的早退分支不再一行日志都没有；`refused` 与「根本没有旧命名空间」从同形变为两个可区分取值。
- **内存窗口旗标**：结论记录后清零（避免后续窗口重复并入同一份已解决的内存态、重复记「该窗口理论不可达」warn）；只有 `fold failed` 保持置位——那些写入确实仍在内存里，下次挂载必须重试。
- **消除二次读**：激活分支的 warn 文案取自**第一次读**返回的理由码（`attached` / `refused` / `no-register` / `not-active`），不再第二次读服务（两次读之间服务可能出现，那样文案会描述一个已不成立的状态）；webServer 站点同样改理由码（`mounted` / `no-register` / `not-active`）。
- **死参数清理**：`attach()` 的第三参数 `unavailableDetail` 无人传，连同那条描述不存在路由的注释一并删除。

### 红线与不变量

`PolicyConfig` schema、投递门、`source` 三成员、`writerGate` / `retireGate`、`inject` 数组（仍 4 项）**全部零改动**；`goals.resume` 仍为**零调用**（换届只建议继任者自行 resume）。

### 验证

- `node host-half.test.mjs` → **506 项全绿**；`node client-half.test.mjs` → **46 项全绿**（基线 447 + 46 = 493）。
- **变异验证**：把 lib 的三处修复逐条回退 → `506 (failed: 4)`；把 `createPolicyStore` 换回真正的修复前形状（0.3.6 版：静默内存回退 + apply 当场跑迁移）→ `506 (failed: 16)`。还原即全绿（每次都按 sha256 校验还原）。
- **真机验证（重启后）**：`team_link_watch register` 后 `profile/settings.yaml` **首次出现 `team-link:` 段**（mtime 变化、10498 → 10838 字节），修复前同样操作 mtime 一动不动——这同时把根因链中原本标注为「推断」的一环变成实证；零手工建队 `drill-verify` → 返回「coordinator 已由创建会话认领」→ **紧接着 `set-role` 成功**（修复前这一步必被写权限门拒绝）。
- **独立复核**：一轮差异审计（explore 子代理，只读）独立复现了上述计数与红绿证据，并逐项核对了 inject 恰 4 项、`migrateLegacyPolicy` 唯一调用点在 attach 内、两个门一行未动、越界改动为零；三轮代码评审全部 PASS。

### 未验证 / 显式推迟（如实标注）

- **provider 在本插件 fiber 存活期间消失、而 scope 取自本插件自身 ctx 时不释放**——与 webServer 站点同形，不属红线，记为已知残留；
- **外部手改 `settings.yaml` 是否触发 watcher**：README 曾标为未验证，**2026-09-18 已实测**（外部删除 `team-link:` 段后 `roster get` 立刻回到 0 团队）；
- 两个 🔵 卫生项显式推迟并留档（store 返回对象里的死导出 `migrateLegacyPolicy`；webServer 站点的一次性门是进程寿命级），见设计 §9.6 ⑪；
- §7-4 的夜班日志历史取证（复核一次静默事件的具体触发器）未做——设计里本就标为可选取证。

---

## 0.3.6 — 2026-09-18（发布前收口于 0.3.7）

修两个真实使用缺陷。背景是一次协调者实测：把目标 id 打错一位（`75ae9099` → `75ae9095`），发送返回「没有活动代理」，于是 ① 调用方没有任何可核对的线索；② 该失败被当成普通回执、继续向上汇报「已发送」——而目标会话其实活着。

- **修 1 · no-agent 文案自愈化**（单目标 / fan-out / 插件内部通知三条路径共用 `deliverToTarget`）：拒绝文案首前缀改为 **`❌ 未投递`**（失败不许被读成已排队），保留原句「目标会话 `<id>` 没有活动代理」，并**新增同工作区存活会话列表**（`ctx.agents.list()` 纯注册表读取——零 surface 读取、无性能代价；过滤 = 根代理 + `origin !== "subagent"` + `cwd === 调用者 agentCwd` + 非自身；每行 `id（运行中/空闲）`，上限 **10** 个，超出时注明「共 N 个，仅列前 10 个」；无匹配输出「当前工作区无其他存活会话。」）与**核对提示行**（对照 id / 刚重启 DSH 时在侧边栏打开目标会话一次 / 先调 `team_link_list_sessions`）。执行上下文无会话身份时跳过 cwd 过滤、只排除自身。
- **修 2 · fan-out 失败领先**：只要存在任一目标 outcome ≠ `delivered`，返回文案**第一行**即为 `❌ N 个目标未投递（M 个已投递）`；**全部投递成功时文案形状一字不变**。no-holder 计入该行但汇总里仍保持独立桶、用词只说「未投递」不说「失败」。
- **行为面改动如实标注**：插件内部通知（rotation-freeze / -done / -cancelled / -expired）当收件人已无活动代理时同一行也会带 ❌ 前缀；`deliverToTarget` 是三条路径的共同出口，**未改动任何投递门与投递语义**。
- **验证**：`host-half` 431 → **447** 项（新增 16 项，当次实测），`client-half` 46 项不变（合计 477 → **493**）；既有断言仅 1 项随文案更新（no-agent 行前缀），其余 430 项零回归。

## 0.3.5 — 2026-09-18（发布前收口于 0.3.7）

修 `team_link_list_sessions` 在真实工作区的超时（生产实测：26 个会话的工作区里该工具超过 60s 工具预算；roster / watch 等其他工具正常）。

- **根因**：surface 读取对**列表每一行**（`LIST_LIMIT` = 50）逐个串行执行，而每个冷会话 = 一次 zstd 日志解压 + 一次表面投影。stub 实测（每次读 250ms）：26 会话串行 **6655ms**、修复后 **256ms**。
- **修法三件**：**有界**（恢复只读前 `PREVIEW_SESSIONS` = **12** 行，第 13 行起活性行降级为「未读（超出快照窗口 12）」，行本身照旧列出并经如实标注）；**并行**（这 12 次读取改 `Promise.allSettled`，一行不可读只降级该行）；**回归锁**（新增 7 项断言：调用次数恰为 12、12 次在首次 resolve 前已全部在飞、降级文案形状、窗口外行仍保留无 surface 的面等）。
- **代价（如实标注）**：窗口外的行不再给出 verdict——其中 `dead` 仍可从行首的「✕ 未运行」读出（运行状态来自 agent 注册表，不经日志），而 `goal-disarmed` 在窗口外不可得，需要时单读该会话（`team_link_export`）。
- **验证**：把 lib 改回串行无界的旧形状后 7 项中 3 项当场变红，还原即全绿；活性信号的计算逻辑（verdict 判定表）逐字未动。`host-half` 424 → **431** 项。

## 0.3.4 — 2026-09-18（发布前收口于 0.3.7）

两阶段换届 rotation（设计 §3.6 全节 + §3.6.1 四原则 + §4.1 + §5.1 U6 + §5.3 红线）。

- **`prepare`（Phase A，仅该角色现任会话）**：10 分钟速率限制防换届风暴；生成一次性令牌（`randomUUID()`，绑定 `(team, role, successor)`，30 分钟 TTL，成功认领即作废）；把 `pairs` / `trustedSenders` / `rememberTargets` / roster 全量快照进 `rotationBackup`；向全部在任成员广播 `[rotation-freeze]` 固定冻结清单；返回一次性明文令牌 + 掩码 + 交接指引。
- **`claim`（Phase B，仅 pending 指定的继任者凭令牌）**：单个多选对话框列出全部「退役者↔同 team 成员」候选 pairs 逐项勾选（对端在团队外的不进候选但点名）；在场确认 = ratified → 正式迁移；超时 / 无确认服务 / 失败 = 无人值守 → 全部域内候选以 provisional 迁移并开 **24h** 回退窗口；迁移 = 删旧 pair + 新建 `{a: 新任, b: 对端, provisional, expiresAt}`；**对称撤销** = 退役者持有的 pairs / trustedSenders / rememberTargets 同步清除；落定与迁移在**同一笔写入**，随后才清 pending——崩溃在两者之间时同一令牌重放只补收尾、不重复迁移。
- **到期清扫**（挂看门狗同一巡逻定时器 + 每次 roster / rotate / team_read 惰性检查）：30 分钟未认领 → 清 pending + `[rotation-cancelled]`（旧任仍为 current）；24h 未批准 → 删迁移出的 pairs + 版本史记 `provisional 未批准过期` + `[rotation-expired]`（新任保持 current，信任回退为过门）。
- **内部广播路径**：四种通知正文是**插件常量**（只插值团队/角色/会话 id/读数/状态词，且先过单行清洗）；免发送方审批但**照走接收方 inbound 策略与 `blockedSenders`**；一律不弹确认框。
- **令牌掩码**：镜像与 `roster get` 的 pending 一律渲染 `tok-<前4>…<后4>`，明文只在 prepare 的一次性返回里出现。
- **provisional 可见面**：send 返回文案后缀、`rotation-done` 状态词、roster get 的 pending/provisional 行、`list_sessions` 的「provisional 配对 N 条」标记；banner 与 `source`（仍恰好三成员）都不加字段。
- **补批准入口 = 设置 UI**：24h 内把该 pair 的 `provisional` 置 `false` 即转正式。
- **评审修复轮（7 项）**：**#1**（🔴）幂等判据从「迁移清单非空」改为 `current === pending.session`——域内无候选时标记本为空数组，旧判据会把已落定 roster 当未认领令牌，重放会走完整迁移、把继任者自己当退役者吊销其正式 pairs；**#2** 补批准后窗口静默关闭；**#3** 内部通知的 ask 分支改为逐目标 refused 行（不再 await 3 分钟确认）；**#4** claim 的镜像改用清 pending 后的数组渲染；**#5** 域内无候选时启用独立状态词 `无待迁移对`；**#6** `rotation-done` / `-cancelled` 的收件人并集加入 `rotationBackup.roster` 里的旧任；**#7** README 清扫措辞改条件表述 + `team_read` 加惰性 sweep。
- **第二修复轮（3 项）**：**#8**（🟡）过期 provisional pair 的 TTL 执行缺口——投递侧守门（`pairRecordBetween` 视过期记录为无 pair）+ 清扫侧把 doomed pairs 的计算与删除提到角色记账判据之前；**#9**（🟡）`set-role` 把角色交给 pending 继任者时清 pending（令牌随身份显式变更失效）；**#10**（🔵）`rotationStatus` 显式记状态词供重放直读；**R8** 顺带修掉一处既有墙钟竞态断言。
- **验证**：净增 74 项（311 → 385）→ 修复轮 +20（405）→ 第二修复轮 +19（424）；`client-half` 42 → 46。三轮修复共 22 项断言做过变异验证（挖洞后分别红 16 / 10 / 10 项，还原即全绿）。

## 0.3.3 — 2026-09-18（发布前收口于 0.3.7）

广播 fan-out + 结构化信封 banner + busy 预判（设计 §3.4 / §3.5 / §4.1 / §5.1 U5、U7）。

- **`targets`**（与 `targetSessionId` 互斥）：寻址优先级「会话 id 直达 > `team:<name>/<role>` > `team:<name>/*`」。`team:<name>/*` **仅该团队现任协调者可发**（策展理由）；角色空缺 → 类型化 `no-holder`（不算投递也不算失败）；团队不在 roster 或形状非法 → 整次调用拒绝（不做「半发」）；fan-out **不放宽任何门**；单次 ≤**8** 目标；重复 id 去重。
- **`meta` 信封**：`{type?, pri?, ref?}` 渲染进 banner 首行紧凑字段；`ref` 超 16 字符按码点截断并注明；枚举外的值 / 未定义字段 / 非对象 / 空 ref / 含换行 ref 一律明确参数错误；**`source` 仍是恰好三成员**。
- **busy 预判**：投递后追加目标忙碌状态（运行中 → 「目标回合已运行 N 分钟（steer 注入当前回合）；需新回合语义请等其空闲」，`N` 读不到时只给 steer 语义）。
- **顺带 4 项 M2 评审质量修复**：**R1** retire 信任清理改为对话框确认后**重新读再按最新视图过滤写回**（读-改-写窗口缩到毫秒级）；**R2** TTL 断言不再依赖墙钟；**R3** 补 `applyRetire` 两条错误分支断言；**R4** `team_read` 的 `decisions` baseHash 标注为「仅供参考/审计」。
- **验证**：`host-half` 净增 76 项（235 → 311），`client-half` 42 项不变（合计 353）。

## 0.3.2 — 2026-09-18（发布前收口于 0.3.7）

roster（团队身份注册表）+ 团队黑板（设计 §3.3 全节 + §4.1 + §5.1 U4）。

- 新增 `team_link_roster`（get / upsert-team / set-role / retire）、`team_link_team_read`、`team_link_team_append`；设置命名空间新增 `teams` 键。
- **写权限**：`writer=coordinator`（默认）时只有该团队 coordinator 现任会话可写，现任空缺时会话路径一律拒绝；`writer=any` 时任何会话可写；读永远开放。
- **`retire`**：current 置空 + 版本史记退役；随后可选**一个**确认对话框列出全部指向退役会话的 `pairs` / `trustedSenders` / `rememberTargets`，确认才清理（无确认服务则跳过清理、仅退役并说明）。
- **镜像**：每次 roster 变更在同一调用内 best-effort 写 `<workspace>/team/<name>/roster.md`（失败只告警——settings 始终是事实源）。
- **黑板**：`decisions.md` 只追加（`seq | ISO 时间 | author-session-id | 正文`，seq 单调递增）、`discipline.md` 整文件替换（必须携带 `baseHash`），两者单行上限 **500** 字符（按码点）；黑板**无写权限门**，写入者记在行内 author。团队名 `[a-z0-9-]+` 白名单 + file 枚举白名单（一律 `path.join`，防路径穿越）。
- **验证**：净增 67 项（168 → 235），`client-half` 42 项不变（合计 277）；变异验证：把写权限门与 baseHash 乐观锁各打一个洞后红 8 项，还原即全绿。

## 0.3.1 — 2026-09-18（发布前收口于 0.3.7）

活性面 + 跨会话看门狗最小版（设计 §3.1 / §3.2 / §3.7）。

- `list_sessions` 每个会话行新增 `活性：` 信号行（verdict 五态、goal phase/activation/轮次与 blockedReason、静默时长、读数时间戳）；goal 状态经 `ctx.get("goals")` **可选注入**（服务缺失显示 `?`，插件功能完整降级）。
- 新增 `team_link_watch`（register / list / clear）：只能给自己注册、拒绝自指、单会话 ≤3 个、`silentMinutes>=10` / `intervalMinutes>=5` / `ttlHours<=24`（默认 12，到点自清）。
- **四态巡逻**：观察者运行中或自身 armed-active 不 tick；目标 armed-active 不 tick；目标 active-but-disarmed **立即** tick 且载荷带诊断与合规 resume 回路；paused/blocked/complete 不 tick；无 goal 且静默超阈才 tick。
- tick 的 `source` 仍是恰好三成员（id 前缀 `slp-wd-`），正文是**插件常量模板**；去抖与「观察者=dead」为进程内状态、不持久化；巡逻定时器随 dispose 清理。
- **验证**：净增 93 项（75 → 168），`client-half` 42 项不变（合计 210）。

## 0.3.0 — 2026-09-17

**更名** `dsh-team-link`（原 `dsh-session-link-pro`）：包名 / cordis 名 / client bundle id / 工具名（`team_link_list_sessions` / `team_link_export` / `team_link_send`）/ 设置命名空间（`team-link`，含旧数据一次性迁移）/ 导出路由（`/team-link/export`）。

**不变量**：`slp-` 消息 id 前缀、`dsh://` 深链协议、上游深链解析行为、双门投递语义——一律保持。

**GitHub 仓库名**于 2026-09-18 一并改为 `dsh-team-link`（旧地址自动重定向）。

## 0.2.4 — 2026-09-17

修「孤立代理项」截断 bug（详见 README 第八节）：

- `preview()` / `truncate()` 改为**按码点截断**（原 `slice()` 按 UTF-16 code unit 切，emoji 落在刀口上只剩一半，会**永久毒死**调用方会话）；「已截断 N 字符」计数口径随之变为码点；
- 新增 `wellFormed()` 并消毒列表正文、export 的 md+JSON、send 的两处批准提问正文、投递到目标会话的 banner、拒绝文本里回显的 `targetId`、深链注入的会话快照与三个工具的 `output.render` 出口；
- 顺带修「resolver 省略可选 `additionalContext` 时把 `undefined` 塞进消息数组」；
- 客户端 `shortSessionId()` 改码点截断，卡片正文 / 发送方 id / 委托回退文本渲染前消毒。
- **验证**：`host-half` 新增 20 项、`client-half` 新增 11 项；把两个 lib 文件换回 0.2.3 时 host 红 13 项、client 红 5 项，换回修复版即 117 项全绿。

## 0.2.3 — 2026-09-17

适配 DSH 0.1.5 会话格式迁移：

- 投递消息 `source` 改为受审计的 `{ kind: "agent-message", form: "relay", senderSessionId }`（旧 `kind: "team-link"` 会让整份会话日志无法迁移/打不开）；
- 卡片判定改从 chat node 的 `node.id` 读消息 id（context 的 `data` 里没有 id），叠加 `slp-` 与正文 banner 双信号，避免把上游相邻代理消息误渲染成本插件卡片；
- 时间改为优先用 context node 的事件时间，正文 banner 承载投递时间兜底；
- 宿主包从 `dependencies` 移到 `peerDependencies`，区间补上 `^0.1.5-rc.1` 分支（`^0.1.0-rc.6` 按 semver 预发布规则匹配不到 `0.1.5-rc.1`）；
- 新增 `client-half.test.mjs`；导出路由文件名净化、策略写入去重。

## 0.2.2 — 2026-09-17

配对通道（双向免确认）；醒目 📡 消息卡片（keyed slot 影子渲染 + 委托回退）；消息 `source` 补 `form: relay` + `senderSessionId` 元数据。

## 0.2.1 — 2026-09-17

空闲目标投递改用 `followup` 唤醒（原 `inject` 只排队不唤醒，用户确认后目标无反应）。

## 0.2.0 — 2026-09-16

初版 fork：会话深链 + 会话列表 / 导出 + 批准式跨会话消息。
